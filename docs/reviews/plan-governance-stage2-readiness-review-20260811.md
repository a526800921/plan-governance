# 计划治理阶段 2 准入复核

> 当前有效结论（阶段 2 完成验收复核，2026-08-11）：通过，阶段 2 已完成；下方保留历轮准入和验收记录。

## 复核信息

| 字段 | 内容 |
|---|---|
| 日期 | 2026-08-11 |
| 范围 | `plan-governance-operability-optimization` 阶段 2 准入准备 |
| 复核者 | Hume（独立只读复核 subagent） |
| 复核方式 | 只读检查阶段 2 Step 0、R1—R7 样本、候选契约、`PLAN_MAP.md`、严格治理、反向引用和前后 hash；未修改工作区 |

## 结论

未通过阶段 2 `待实施` 准入。现有阶段 2 Step 0 能说明方向和边界，但还不能复现每个关系样本的输入、输出、退出码和失败判定。

## 已通过项目

- `node bin/plan-governance-cli.mjs check . --strict-readiness` 退出码为 0，仅有两个活跃计划共享影响目标的预期 WARNING。
- 关系候选设计的事实源、当前阶段指针和共享写入串行边界与两个计划没有语义冲突。
- 相关文档本地链接 68 条，缺失 0 条；事实源扫描未发现旧草案重新成为规范事实源。
- `PLAN_MAP.md`、专项计划、阶段 2 fixture 的前后 hash 和 Git 工作区状态保持不变。

## 未通过原因

1. R1—R7 主要是设计描述和通用 `rg` 命令，没有每个案例的可执行关系输入、预期输出、失败退出码和输出位置。
2. 引用错误没有明确覆盖来源计划/来源阶段缺失；环依赖没有明确自环、多节点环和 `soft_context` 排除规则。
3. 旧计划级依赖和九列表 `shared_write_risk` 尚无真实兼容样本；当前 `PLAN_MAP.md` 仍是四列表人工说明，缺少混用/缺失规则。
4. “复用 `check`、不新增关系命令”的方向合理，但字段必填、冲突 WARNING 和非法关系是否阻止 `workset` 确定并行结论仍需冻结。

## 后续准入条件

- 为 R1—R7 补充真实临时输入、执行命令、预期结果、失败退出码和输出位置。
- 明确九列表 `shared_write_risk` 与现有四列表的兼容和混用规则。
- 明确环检测范围、字段必填规则、旧依赖冲突告警和非法关系对 `workset` 的影响。
- 修订后重新进行不参与实现的独立准入复核；通过前不得推进 `PLAN_MAP.md` 当前阶段或开始阶段 2 实现。

## 第二轮复核（当前有效，仍未通过）

### 复核信息

| 字段 | 内容 |
|---|---|
| 日期 | 2026-08-11 |
| 范围 | 阶段 2 修订后的 R1—R7、候选实现、兼容规则和治理证据 |
| 复核者 | Wegener（独立只读复核 subagent） |
| 复核方式 | 只读运行阶段 2 测试、npm/Python 全量测试、严格治理、workset 降级、反向引用和状态检查；未修改工作区 |

### 结论

仍未通过阶段 2 `待实施` 准入。实现方向和大部分验证已通过，但仍有以下准入缺口：

- valid fixture 尚未实际包含 `soft_context` 与 `evidence` 行，R2 与 R1 重复；
- R5 没有单独验证默认模式、冲突 WARNING、workset 和旧依赖不被覆盖；
- 阶段引用只检查格式，尚未核对阶段路线图是否真实存在；
- 缺失解除条件/证据的正反样本不完整；
- 四列表与九列表的事实优先级、混用和冲突规则尚未写清；
- fixture 需要测试辅助函数复制到 `<root>/docs/PLAN_MAP.md` 后才能运行，直接回放路径不够清晰；
- 计划中的阶段 1 历史测试指标与当前候选实现指标尚未分层记录；
- 阶段 2 准入前出现候选实现，已记录为流程偏差，不能据此自动放行。

### 已通过证据

- 阶段 2 相关测试 5 项通过；该轮全量快照为 `npm test` 39/39、Python 102 passed、覆盖率 90.56%。
- 默认/严格治理、`--stale-days 10`、非法关系下 `workset.parallel.state=unknown` 和 R7 hash 通过。
- 69 条本地链接无缺失，`git diff --check` 通过；当前阶段仍为阶段 1，阶段 2 仍为设计中。

## 第三轮复核记录

| 日期 | 结论 | 证据 | 复核者 |
|---|---|---|---|
| - | 待修订后复核 | 本报告后续追加 | - |
| 2026-08-11 | 未通过：候选实现可运行，但阶段 2 准入材料和证据仍不完整 | 本节第二轮复核 | Wegener（独立只读复核 subagent） |

## 第三轮复核（当前有效，仍未通过）

### 复核信息

| 字段 | 内容 |
|---|---|
| 日期 | 2026-08-11 |
| 范围 | 阶段 2 关系校验候选实现、R1—R7 fixture、兼容边界和独立准入证据 |
| 复核者 | Volta（独立只读复核 subagent） |
| 复核方式 | 只读运行阶段 2 fixture、全量测试、严格治理、workset 降级、反向引用、共享写入约束和 hash 检查；未修改工作区 |

### 结论

仍未通过阶段 2 `待实施` 准入。实现方向、fixture 可直接回放、错误退出码、严格治理、非法关系下 `workset.parallel.state=unknown`、只读 hash、反向引用和共享写入约束均已通过；但复核发现以下证据仍需补强：

- 合法 fixture 虽包含三种关系类型，但测试还需要更明确断言 `hard_gate`、`evidence`、`soft_context` 的行为差异。
- 旧计划兼容测试需要同时证明普通 legacy 的默认检查和 workset 行为，以及冲突场景中的额外旧依赖不会被覆盖。
- 阶段路线图引用、证据链接存在性和四列/九列表优先级需要有明确的反例断言。
- 计划和复核材料中的候选实现指标需要更新为当前实际回归结果。

### 复核后的本地修订回归

复核意见对应的 fixture、断言和证据路径检查已补齐。当前本地回归结果为：

- `python3 -m pytest -q`：106 passed，覆盖率 90.66%。
- `npm test`：39/39。
- `node bin/plan-governance-cli.mjs check . --strict-readiness`：退出码 0；仅保留两个活跃计划共享影响目标的预期 WARNING。
- `git diff --check`、Python 编译检查和 Node 语法检查通过。

以上是修订后的实施者回归，不替代第四轮独立准入复核；在第四轮明确“达到 `待实施` 标准”前，`PLAN_MAP.md` 当前阶段仍保持阶段 1。

## 第四轮复核（历史，未通过）

### 复核信息

| 字段 | 内容 |
|---|---|
| 日期 | 2026-08-11 |
| 范围 | 阶段 2 完整准入条件、R1—R7 可执行命令、实现回归和反向引用 |
| 复核者 | Boyle（独立只读复核 subagent） |
| 复核方式 | 只读运行 R1—R7、全量 Python/npm 测试、严格治理、停滞检查、语法/格式/链接/反向引用和 hash 检查；未修改工作区 |

### 结论

未通过阶段 2 `待实施` 准入。实现和大部分证据已满足要求，仅发现两个明确文档准入缺口：

1. 阶段 2 准入准备缺少独立的“完成条件”章节；阶段准入规则要求验证方式、完成条件、失败策略和回滚边界同时存在。
2. R6 样本命令只匹配非法九列表测试，没有覆盖四列表 legacy 兼容和四列/九列冲突优先级两个已声明 fixture。

其余 R1—R7 行为、严格治理、旧依赖保留、阶段真实引用、证据路径、workset 降级、只读 hash、全量测试、覆盖率和反向引用均通过。修订动作是补齐阶段 2 完成条件，并把 R6 命令扩展为同时匹配三个共享写入测试；修订后再进行一次最终独立复核。

## 第四轮修订后最终确认（当前有效，通过）

### 复核信息

| 字段 | 内容 |
|---|---|
| 日期 | 2026-08-11 |
| 范围 | 阶段 2 两项文档修订及 R1—R7、全量回归结果 |
| 复核者 | Boyle（独立只读复核 subagent） |
| 复核方式 | 只读确认阶段 2 完成条件、R6 三测试命令覆盖、阶段准入结构和回归结果；未修改文件、未提交、未同步全局环境 |

### 结论

通过，达到阶段 2 `待实施` 标准。阶段 2 已具备正式实施条件：

- 阶段 2 已新增明确的“完成条件”，覆盖关系契约、兼容性、测试覆盖率、治理检查、反向引用、hash、状态同步和独立验收要求。
- R6 命令已覆盖非法九列表、四列表 legacy 兼容和四列/九列冲突优先级三个测试，定向结果为 `3 passed, 69 deselected`；阶段 2 相关定向回归为 `9 passed, 63 deselected`。
- 最终回归为 Python `106 passed`、覆盖率 `90.66%`，npm `39/39`，严格治理和停滞检查退出码均为 0，Node/Python 语法检查和 `git diff --check` 通过。
- R1—R7、旧依赖兼容、阶段真实引用、证据路径、`workset` 降级、只读 hash、反向引用和事实源扫描均已确认；工作区未被本轮复核修改。

本结论只放行阶段 2 的正式实施，不改变阶段 3 的准入状态；阶段 2 完成后仍需独立验收。

## 阶段 2 完成验收（首次复核，历史未通过）

### 复核信息

| 字段 | 内容 |
|---|---|
| 日期 | 2026-08-11 |
| 范围 | 阶段 2 完成条件、实施回归、R1—R7 和治理状态同步 |
| 复核者 | Banach（独立只读复核 subagent） |
| 复核方式 | 只读检查计划、`PLAN_MAP.md`、阶段 2 完成条件、全量测试、治理、链接、反向引用和 hash；未修改文件或全局环境 |

### 结论

技术项全部通过，但首次复核未通过阶段完成判定：阶段状态仍是“实施中/待独立验收”，`PLAN_MAP.md` 还没有阶段 2 完成证据和最终验收记录。该问题已在本计划和 `PLAN_MAP.md` 中同步修正。

## 阶段 2 完成验收复核（当前有效，通过）

### 复核信息

| 字段 | 内容 |
|---|---|
| 日期 | 2026-08-11 |
| 范围 | 阶段 2 完成后的状态、完成证据、验收记录和阶段 3 边界 |
| 复核者 | Banach（独立只读复核 subagent） |
| 复核方式 | 只读确认阶段路线图、当前阶段摘要、完成验收记录、`PLAN_MAP.md` 状态/当前阶段/完成证据/链接，以及严格治理、停滞检查、链接检查和 `git diff --check`；未修改文件或全局环境 |

### 结论

通过，阶段 2 完成。已确认：

- 阶段路线图和当前阶段摘要均为“已完成”。
- 阶段 2 完成验收记录已写入专项计划。
- `PLAN_MAP.md` 的计划状态、当前阶段、完成证据和链接已同步。
- 阶段 3 仍为“设计中”，未自动放行。
- 严格治理、停滞检查、链接检查和 `git diff --check` 均通过。

本结论关闭阶段 2，不代表阶段 3 已具备实施条件。
