# 计划治理阶段 0 独立复核

## 复核信息

| 字段 | 内容 |
|---|---|
| 日期 | 2026-08-10 |
| 范围 | `plan-governance-operability-optimization`、`autonomous-plan-execution` 阶段 0 |
| 复核者 | Dalton（独立只读复核 subagent） |
| 复核方式 | 独立阅读治理文档、源代码和设计案例；运行治理检查、测试和反向引用检查；不修改工作区 |

## 结论

方向通过，但尚未达到阶段 1 `待实施` 标准。事实源分工、阶段 0 设计并行/阶段 1 上游硬门禁、共享写入需串行等方向成立；当前仍存在契约冲突、未冻结的技术映射和不足以证明完成条件的失败样本。

## 阻塞问题

1. 自主计划的“唯一可执行下一步”与技术稿/案例中的多个 `ready_steps` 冲突，必须统一为“当前可执行步骤集合”。
2. 工作集派生优先级、旧计划级依赖到阶段关系的兼容投影、显式阶段证据声明位置和解析规则仍不够确定。
3. Attestation 的现有存储固定为 `docs/attestations/<plan>.json`，但新关系设计尚未确定快照 ID、文件命名、多 purpose 共存和 hash 漂移后的有效状态。
4. 自主步骤案例缺少动作、完成条件、分支记录、重复 ID、环依赖、缺失证据和非法状态转换等失败输入。
5. 两个计划的设计案例共享文件所有权不清，当前治理检查对共享影响目标持续输出 WARNING。

## 非阻塞问题

- 阶段关系方向合理，没有形成循环门禁。
- `PLAN_MAP.md` 的一处阶段路线图链接锚点需要修正。
- 可分发 skill 与已安装 skill 一致；未发现旧草案重新成为事实源。

## 验证证据

- `plan-governance-cli check . --strict-readiness`：退出码 0；存在共享目标重叠 WARNING。
- `npm test`：37/37 通过。
- `git diff --check`：通过。
- Drift 和 Attestation 检查均退出码 0，但现有工作区存在预期的共享目标和 hash 漂移 WARNING。
- 目标样本 `motorcycle-manual-app` 只读回放产生大量当前工作区变更 WARNING，不能替代隔离 fixture。

本报告是独立复核证据，不是阶段通过结论；修复阻塞项后必须追加新的独立复核记录，不能覆盖本记录。

## 第二轮修订复核

| 字段 | 内容 |
|---|---|
| 日期 | 2026-08-10 |
| 结论 | 通过：修订后的阶段 0 设计达到阶段 1 `待实施` 准入要求；阶段 1 仍须遵守自身 Step 0、验证方式、完成条件和阶段转换规则 |
| 复核者 | Dalton（独立只读复核 subagent） |
| 验证 | `plan-governance-cli check . --strict-readiness`、`npm test` 37/37、`git diff --check`、链接和反向引用检查通过 |
| 剩余问题 | 无阻塞项；可后续补充 serial 策略案例和 superseded 派生状态的更细说明 |

本轮复核通过的是修订后的阶段 0 设计，不代表已实施阶段 1，也不允许跳过阶段 1 自身准入。
