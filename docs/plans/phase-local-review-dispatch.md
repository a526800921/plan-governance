# 计划：phase-local-review-dispatch

## 背景

当前 `plan-governance` 的跨轮持续推进由 Codex `goal` 承载；旧的“自主连续执行”计划已经废弃。现有阶段准入规则要求当前阶段在进入 `待实施` 前必须有独立准入复核，但没有规定执行者在小阶段到达该门禁时可以自动派发复核者。阶段关系、当前工作集和证据状态沿用已完成的 [plan-governance-operability-optimization](plan-governance-operability-optimization.md) 计划。

因此，计划执行会在一个通常不需要用户决策的小阶段停下来等待独立复核。这个停顿把“需要独立判断”误表现成“需要用户确认”，降低了连续推进体验；但直接恢复旧的整计划自主执行又会重新引入已废弃的执行模式，并可能越过高影响决策边界。

本计划只处理“阶段内独立复核的调度与恢复”这一局部问题：普通、目标明确且不涉及高影响决策的阶段，执行者应自动启动独立只读复核 subagent 或等价复核者，复核通过后继续推进；高影响事项、复核失败、复核不可用或证据冲突仍必须停下并报告。

## 目标

- 为阶段内独立准入复核建立“自动派发、复核返回、继续或阻塞”的执行契约。
- 让普通且目标明确的阶段在等待独立复核时不再无谓等待用户。
- 保留独立复核的真实性：实施者不能自我批准，复核必须基于当前仓库、可复现命令和阶段完成条件。
- 明确必须询问用户的高影响边界、复核失败策略、复核不可用策略和恢复条件。
- 与现有 `goal` 跨轮持续推进、阶段准入、`PLAN_MAP.md` 和历史计划兼容，不恢复旧的整计划自主执行入口。

## 非目标

- 不恢复或复活已废弃的整计划自主执行历史计划。
- 不新增 `等待独立准入复核` 计划生命周期状态；现有 `设计中`、`待实施`、`实施中` 等状态保持不变。
- 不自动批准独立复核，不把治理检查、全量测试或实施者声明当作复核结论。
- 不自动替用户决定范围、产品取舍、外部授权、凭证、公开暴露、破坏性操作、安全或隐私边界。
- 不构建通用工作流引擎，不自动执行未在当前计划和授权范围内的外部操作。

## 需求探索

### 已确认事实

- 之前确认的目标是“整个计划连续执行”，当前实际卡点收窄为“计划内部的小阶段在等待独立准入复核时无谓停止”。
- 对普通、目标清楚且不需要用户作高影响决策的小阶段，通常没有必要停下来等待用户确认。
- 遇到独立准入复核时，可以由执行者自行启动独立只读 subagent 或等价复核者；该复核不能由实施者自己批准。
- 独立复核通过后应继续当前计划推进；复核未通过、不可用或证据冲突时，应保留阻塞并报告，而不是自行放行。
- 本计划只解决阶段内复核调度，不恢复旧的整计划自主执行模型。

### 暂定假设与验证方式

- Codex 当前的独立任务/agent 调度能力可以承载一个新上下文的只读复核任务；若实际运行环境不支持自动派发，需要定义等价的复核调用或明确阻塞。
- “独立”采用程序性边界：复核任务使用独立上下文，不参与当前阶段实施，不修改工作区，并单独执行检查命令；这不等同于组织或合规意义上的第三方审批。
- 阶段内复核的完成结果由复核报告和计划记录承载；不把复核进行中增加为 `PLAN_MAP.md` 的生命周期状态。通过普通阶段、复核通过、复核未通过、复核不可用和高影响决策五类场景验证。
- 当前仓库没有计划执行 runtime 的实现代码，现状基线先采用 skill/README/历史计划的规则快照；阶段 1 再确认实际宿主调度入口和可回放的运行 fixture。

### 范围与非目标

本计划范围是阶段准入门禁触发后的复核任务派发、独立性约束、结果消费、失败/不可用处理和继续推进边界。范围包括需要同步的 skill、代理运行说明、计划模板或复核记录字段，以及对应的测试和验证 fixture。

不在范围内的是重新设计计划生命周期、恢复 `plan next` 或步骤清单入口、建立多人审批流、自动通过高影响门禁、替代 Codex `goal`，以及对历史计划做全量回填。

### 候选方案与取舍

| 方案 | 处理方式 | 取舍 |
|---|---|---|
| A | 保持当前行为，所有独立复核都停下来等用户 | 最安全但延续无谓停顿，不符合已确认的连续推进目标 |
| B | 增加 `等待独立准入复核` 生命周期状态 | 可读性较强，但扩大状态机并违背现有“不新增计划状态”边界 |
| C | 阶段门触发时自动派发独立只读 subagent，结果通过后继续 | 推荐；解决实际卡点，保留独立门禁和高影响停机边界 |
| D | 恢复旧的整计划自主执行机制 | 范围过大，和现有 `goal` 及废弃记录冲突，拒绝采用 |

### 未决问题

| 问题 | 推荐方案 | 是否阻塞当前阶段 | 状态 |
|---|---|---|---|
| 当前宿主如何可靠创建独立只读复核任务并在完成后恢复执行？ | 阶段 1 先验证可用的 subagent/独立任务入口；不可用时保留明确的人工或等价复核回退 | 否 | 待阶段 1 验证 |
| 如何记录“复核者未参与实施”而不伪造强身份认证？ | 记录复核任务标识、复核范围、只读方式、被审查 revision 和实际命令；高风险场景再要求人工复核 | 否 | 设计中 |
| 复核返回后是否在同一运行轮次继续，还是交给 `goal` 恢复？ | 优先同轮继续；跨轮或异步返回时使用 `goal` 保持上下文，不把用户输入当作默认前置条件 | 否 | 待阶段 1 验证 |

### 用户确认的探索结论

2026-09-05，用户确认：此前的“整个计划连续执行”不是当前需要恢复的能力；当前问题是计划内部小阶段会因独立准入复核而停止，而通常不需要用户确认。对于普通、目标明确且无高影响决策的小阶段，应尽可能继续推进，并在需要独立复核时自动启动 subagent 或等价复核者；只有高影响事项、复核失败、复核不可用或证据冲突时才停下请求处理。

## 不变量

- 阶段准入仍是硬门禁；自动派发复核不等于自动放行。
- 实施者不能作为自己的独立准入复核者；复核者必须以独立上下文和只读方式重新检查当前事实。
- 复核通过后才能继续越过该阶段门；复核未通过或不可用时不得自行修改结论或假设为通过。
- 高影响决策和不可逆操作前必须保留用户确认边界；“尽可能继续”不扩大授权范围。
- `PLAN_MAP.md` 继续承载计划级状态、当前阶段、依赖、阻塞项和证据链接；不新增阶段等待状态。
- `docs/plans/*.md` 继续承载阶段实施细节、复核证据和失败/回滚边界。
- 复核报告必须能定位被审查的计划阶段、revision、命令、输出和结论；历史复核追加保留。

## 影响模块或文件

- `resources/skill/SKILL.md`
- `resources/skill/agents/openai.yaml`
- `resources/skill/assets/plan.template.md`
- `scripts/init_plan_governance.py`
- `scripts/check_plan_governance.py`
- `README.md`
- `tests/`
- `docs/PLAN_MAP.md`
- `docs/plans/phase-local-review-dispatch.md`
- `docs/reviews/phase-local-review-dispatch-stage0-independent-review-20260905.md`
- `docs/fixtures/` 或等价的阶段内复核运行记录

实际 runtime 调度入口尚未确认；阶段 0 不假定某个宿主 API 或某个具体 Agent 名称。

## 公共契约变化

本计划拟冻结以下行为契约，具体字段和宿主入口在阶段 0/1 验证后确定：

```text
阶段内当前动作需要独立准入复核
    ├─ 无高影响决策，且复核入口可用
    │    └─ 创建独立只读复核任务
    │          ├─ 通过：记录复核证据，继续当前执行
    │          └─ 未通过：保持设计/阻塞状态，报告缺口
    ├─ 复核入口不可用或证据冲突
    │    └─ 不自我批准，保留阻塞并报告
    └─ 涉及高影响决策或不可逆操作
         └─ 先请求用户确认，再决定是否继续
```

复核任务至少需要返回：计划、阶段、被审查 revision、复核范围、实际执行命令、证据位置、结论、阻塞项和复核者标识。复核任务不修改工作区；主执行者只负责消费和落档该结果，不能改写“未通过”为“通过”。

复核入口不可用时，只有满足同等独立性和证据要求的等价复核者可以替代；没有复核者时必须停止并报告，安全替代动作本身不构成放行条件。

## 阶段路线图

| 阶段 | 目标 | 进入条件 | 验证方向 | 状态 |
|---|---|---|---|---|
| 阶段 0 | 固定问题原因、阶段内复核调度契约、用户确认边界和 Step 0 基线 | 用户已确认“普通阶段自动派复核并继续”的范围 | 规则反向引用、基线命令、失败矩阵和独立设计复核 | 待实施 |
| 阶段 1 | 接入实际执行宿主的复核派发与结果恢复机制 | 阶段 0 独立准入复核通过；宿主入口可定位 | 普通/高影响/通过/失败/不可用场景回放，且不越过现有授权边界 | 候选 |
| 阶段 2 | 同步分发入口并完成阶段内连续推进验收 | 阶段 1 实现与回归通过 | skill、代理规则、模板、CLI/测试和真实运行记录一致 | 候选 |

## 当前阶段

### 范围

阶段 0 只完成原因确认、需求边界、契约草案、Step 0 基线、样本矩阵、失败/回滚边界，并登记阶段 1 的验证入口和定位问题。不要求阶段 1 宿主入口在阶段 0 完成接入；不修改运行时、skill 分发资源、CLI、模板或目标项目。

### 阶段准入摘要

| 字段 | 内容 |
|---|---|
| 准入状态 | 待实施 |
| Step 0 | [阶段 0 Step 0 证据](#阶段-0-step-0-证据) |
| 样本矩阵 | [阶段 0 样本矩阵](#阶段-0-样本矩阵) |
| 验证方式 | [验证方式](#验证方式) |
| 失败/回滚边界 | [风险和回滚](#风险和回滚) |
| 当前阻塞项 | 无；阶段 1 的实际宿主调度入口尚未验证，属于阶段 1 前置条件 |
| 最新独立准入复核 | [2026-09-05 第二轮复核：通过，达到待实施标准](#独立复核记录) |

### 实施步骤

1. 固定当前 skill、README、已废弃自主执行计划和 CLI 的现状基线。
2. 将用户确认的“阶段内自动派复核并继续”与高影响停止边界写成契约。
3. 设计独立复核任务的输入、输出、只读边界、失败策略和恢复方式。
4. 定位实际宿主调度入口，定义阶段 1 的可回放验证 fixture。
5. 由未参与本阶段设计的复核者检查契约、样本矩阵和边界；通过后才进入阶段 1。

### 阶段 0 Step 0 证据

基线类型为“规则源和运行入口现状快照”。当前仓库没有计划执行宿主的实现代码，无法仅通过本仓库 CLI 重现 Codex runtime 的停顿；因此先用规则源、历史计划和治理检查固定原因：

- `resources/skill/SKILL.md` 当前只声明跨轮持续由 `goal` 管理，并不定义阶段内自动派发独立复核。
- 旧的整计划自主执行历史文档已于 2026-08-28 废弃；其历史契约明确不构建自动执行器、不自动批准独立复核。
- `docs/PLAN_MAP.md` 将旧计划标为已废弃，并以 Codex `goal` 作为替代方式。
- 当前基线命令 `plan-governance-cli check . --strict-readiness` 通过；该结果只证明治理结构有效，不证明 runtime 会自动派发复核。
- 以上是仓库规则源的原因基线；用户观察到的 Codex 宿主停顿属于待阶段 1 通过实际入口和场景回放确认的运行时事实，不能由本阶段文档检查代替。

### 阶段 0 样本矩阵

| 样本/fixture | 输入或基线 | 可执行命令 | 预期结果 | 失败判定 | 输出位置 |
|---|---|---|---|---|---|
| 当前持续推进入口 | skill、README、代理规则中的 `goal` 说明 | `rg -n '跨轮持续推进|goal|自主连续执行模式|独立准入复核' resources/skill/SKILL.md README.md` | 能定位跨轮持续、阶段准入和当前缺少复核派发契约的规则 | 将 `goal` 或现有 skill 误读为已支持阶段内自动派发 | 本节命令输出 |
| 旧自主执行边界 | 已废弃计划及 PLAN_MAP 替代记录 | `rg -n '已废弃|替代方式|不自动批准独立复核|自动执行 shell|Codex goal' docs/plans/ docs/PLAN_MAP.md` | 证明旧计划不是当前事实源，且不能直接恢复整计划自动执行 | 新方案重新引入旧入口、自动批准或跳过授权 | 本节命令输出 |
| 阶段准入硬门 | 当前阶段准入规则与检查器 | `rg -n '阶段 N\+1 默认保持|最新独立准入复核明确|ACTIVE =|independent_review' resources/skill/SKILL.md scripts/check_plan_governance.py` | 证明复核仍是硬门禁，但当前代码只派生提示动作，不负责创建复核任务 | 通过测试/状态或实施声明替代独立复核 | 本节命令输出 |
| 普通阶段自动复核契约 | 阶段 0 公共契约中的低风险分支，作为 runtime 尚未接入前的可观察替代基线 | `rg -n '无高影响决策，且复核入口可用|创建独立只读复核任务|通过：记录复核证据，继续当前执行' docs/plans/phase-local-review-dispatch.md` | 能定位自动派发、通过后继续和低风险边界；不宣称 runtime 已实现 | 缺少任一分支，或把契约文本误报为运行通过 | 本节命令输出；阶段 1 运行记录待补 |
| 高影响决策契约 | 公共契约中的外部授权、不可逆操作和高影响分支 | `rg -n '涉及高影响决策或不可逆操作|先请求用户确认' docs/plans/phase-local-review-dispatch.md` | 在动作前请求用户确认，不由 subagent 自动放行 | 未确认即执行外部或不可逆动作 | 本节命令输出；阶段 1 运行记录待补 |
| 复核失败或不可用契约 | 公共契约中的失败、超时、工具不可用分支 | `rg -n '复核入口不可用或证据冲突|不自我批准，保留阻塞并报告|没有复核者时必须停止并报告' docs/plans/phase-local-review-dispatch.md` | 保持阻塞，记录原因，不自我批准；没有等价复核者时停止并报告 | 伪造通过、静默跳过或无限等待 | 本节命令输出；阶段 1 运行记录待补 |
| 证据冲突契约 | 公共契约中的证据冲突分支 | `rg -n '证据冲突|复核入口不可用或证据冲突' docs/plans/phase-local-review-dispatch.md` | 证据冲突归入不可放行分支，不以安全替代动作单独放行 | 忽略冲突、覆盖历史证据或自行改写结论 | 本节命令输出；阶段 1 运行记录待补 |

### 阶段证据

- 当前阶段只记录本计划的需求确认、现状快照和设计证据；阶段 1/2 的实现文件和运行记录不得提前声明为完成证据。阶段 0 已达到 `待实施`，不等同于阶段 1 已接入实际宿主。

### 最近实施/验证记录

| 日期 | 类型 | 动作/结果 | 证据 | 状态 | 记录者 |
|---|---|---|---|---|---|
| 2026-09-05 | 原因核对 | 确认旧自主执行计划已废弃；当前 skill 没有阶段内自动派发复核契约 | `resources/skill/SKILL.md`、`docs/PLAN_MAP.md` 及历史记录 | 通过 | Codex |
| 2026-09-05 | 需求确认 | 用户确认普通小阶段自动派发独立复核并继续，高影响/失败/不可用/冲突时停止 | 本计划“用户确认的探索结论” | 通过 | 用户/Codex |
| 2026-09-05 | 第二轮独立复核 | 第一轮问题修复通过；阶段 0 达到 `待实施` 标准 | [第二轮追加复核记录](../reviews/phase-local-review-dispatch-stage0-independent-review-20260905.md#第二轮复核追加记录) | 通过 | Nietzsche（独立只读复核 subagent） |

最近记录不替代独立准入复核。

### 验证方式

- 阶段 0：运行样本矩阵命令，执行 `plan-governance-cli check . --strict-readiness`，并用 `rg` 检查旧计划、状态名、独立复核和 `goal` 引用没有重新形成冲突事实源。
- 阶段 1：在实际宿主中回放普通阶段、高影响决策、复核通过、复核未通过、复核不可用和证据冲突六类场景；检查复核任务只读、独立上下文、revision 绑定和恢复/阻塞行为。
- 阶段 2：运行 `npm test`、Python 测试、严格治理、必要的 `--stale-days`、`--drift`、`--pre-commit` 和分发安装回归；确认未恢复 `plan next` 或整计划自主执行入口。
- 完成验收：由未参与实现的独立复核者基于当前仓库、可复现命令和运行记录复核，不依据实施者状态声明直接判定完成。

### 测试覆盖率

阶段 0 尚未实施代码，暂无覆盖率声明。阶段 1/2 必须记录新增测试、场景回放和覆盖率证据。

### 完成条件

- 阶段 0 的原因、目标、范围、非目标、独立性和高影响停止边界已冻结。
- 阶段 0 的 Step 0、样本矩阵、失败判定和输出位置可复核。
- 阶段 1 能在普通阶段门触发时自动派发独立只读复核，并在通过后恢复当前推进。
- 复核失败、不可用或证据冲突不会被自我批准或无限等待；高影响事项仍请求用户确认。
- 不新增计划生命周期状态，不恢复旧自主执行入口，不越过现有阶段准入和授权边界。
- skill、代理规则、模板、CLI/测试、计划文档和 `PLAN_MAP.md` 的引用关系同步。
- 独立复核报告、场景回放、测试和治理检查全部通过。

## 最新独立准入复核

| 字段 | 内容 |
|---|---|
| 日期 | 2026-09-05 |
| 阶段 | 阶段 0 |
| 结论 | 通过，达到 `待实施` 标准 |
| 证据 | [阶段 0 第二轮独立复核报告](../reviews/phase-local-review-dispatch-stage0-independent-review-20260905.md#第二轮复核追加记录) |
| 复核者 | Nietzsche（独立只读复核 subagent） |

## 独立复核记录

| 日期 | 类型 | 阶段 | 结论 | 证据 | 复核者 |
|---|---|---|---|---|---|
| 2026-09-05 | 阶段 0 设计准入复核 | 阶段 0 | 未通过：阶段阻塞归属、样本矩阵和不可用回退边界需修订 | [复核报告](../reviews/phase-local-review-dispatch-stage0-independent-review-20260905.md) | Peirce（独立只读复核 subagent） |
| 2026-09-05 | 阶段 0 设计准入复核（第二轮） | 阶段 0 | 通过，达到 `待实施` 标准 | [复核报告第二轮追加记录](../reviews/phase-local-review-dispatch-stage0-independent-review-20260905.md#第二轮复核追加记录) | Nietzsche（独立只读复核 subagent） |

## 未决问题

| 问题 | 推荐方案 | 是否阻塞当前阶段 | 状态 |
|---|---|---|---|
| 实际宿主的 subagent/独立任务调度入口是什么？ | 阶段 1 先做只读能力探测并固定可回放入口；能力不存在时保留人工/等价复核回退 | 否 | 待验证 |
| 高风险计划是否需要人工而不只是 Agent 复核？ | 普通阶段允许独立只读 Agent；发布、安全、合规和不可逆外部操作再由专项计划声明人工复核 | 否 | 设计中 |

## 风险和回滚

风险：自动继续的边界过宽，可能把需要用户决策的事项交给 subagent 自行判断。

控制：将外部授权、凭证、公开暴露、破坏性操作、安全/隐私和产品取舍列为强制停机边界；复核 subagent 只读且不能批准自身或实施者结果。

风险：subagent 不可用、超时或返回结论与仓库证据冲突，执行者为了连续性而静默放行。

控制：失败和不可用均保持阻塞并报告；只有明确的可观察替代复核满足当前计划条件时才允许继续。

回滚：阶段 1/2 发生越权、误放行、无限等待或兼容性回归时，恢复为当前“等待独立复核并报告”的保守行为；保留复核记录和失败证据，不删除历史结论。

## 关联 ADR、迁移、spec 或 issue

- 现有阶段准入契约：[phase-entry-gate-hardening](phase-entry-gate-hardening.md)
- 现有独立验收契约：[independent-acceptance-rules](independent-acceptance-rules.md)
