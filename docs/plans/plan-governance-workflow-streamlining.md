# 计划：协作流程减负与技能结构优化

## 执行交接

- **要解决的问题**：减少低风险任务的复核、重复读取与重复验证，保留高影响检查，并把 AI 技术验证、用户体验及性能验收连成闭环。
- **当前授权**：用户于 2026-09-06 明确要求推进本计划，并将复核分流落实到共享 skill，供后续会话遵循。先完成阶段 0 设计及阶段 1 自身准入，再实施；不重复请求已明确的实施授权。
- **本计划采用的方向**：简单改动当前 AI 自验；高影响保留独立复核；一个 skill 配三份按需参考；项目依赖已安装 skill 或 CLI；用户接受后关闭功能计划。
- **下一步入口**：阶段 1 原技术成果已[独立验收通过](../reviews/plan-governance-workflow-streamlining-completion-review-20260906.md#第三轮通过)，1.0.1 发布及本地更新见[分发维护](plan-governance-distribution-setup.md#101-本地更新)。使用反馈 U1—U5 已[源规则独立复核通过](../reviews/plan-governance-usage-adjustments-review-20260907.md)；本机资源及两个项目入口已同步，并已[发布 1.0.2](plan-governance-distribution-setup.md#2026-09-07-102-发布维护)，用户已确认提问减少及先验证可行性可接受，执行抽查已确认自动复核和定向修复；剩余包括[重复读取](#使用验收与执行抽查2026-09-08)与[调试复核粒度](#调试复核粒度优化提案2026-09-08)，后者已完成源规则实施与独立复核，见[本次实施进展](#本次实施进展)，已[发布并同步本地 1.0.3](plan-governance-distribution-setup.md#2026-09-08-103-发布与本地更新)，其他项目策略未自动迁移。小改不逐次签收；详细范围见[本次增量](#实际使用反馈与有界调整2026-09-07)。
- **这批计划的关系**：前置已提交技术成果与本次推进授权 → 本计划设计收敛 → 实施及用户验收。宿主回放计划已[替代收口](phase-local-review-dispatch.md#收口记录2026-09-09)，旧回放未完成事实保留，不再作为独立支线推进。

## 执行顺序

以下是两个既有阶段内部的工作顺序，不新增阶段、逐项审批或逐项独立复核。本轮已获推进及共享 skill 实施授权；阶段 1 已独立准入，当前实施中。

| 顺序 | 工作及交付 | 结束判据 |
|---|---|---|
| 1 · 阶段 0 | 核对前置最终树、安装版本及未提交差异；按[成果对照](#与前置及相关计划的边界)保留已交付能力，固定剩余差距和代表性旧流程基线 | 已覆盖、部分覆盖、未覆盖可区分；证据可访问，最终交接基线与本次推进授权可定位后解除 B01 |
| 2 · 阶段 0 | 收敛[三项设计交付](#阶段-0-设计交付)：风险复核与阶段门、技术完成与用户接受、规则读取与旧项目兼容 | 给出真实记录方式、旧/新正反样本和受影响文件；不伪造独立通过、不制造额外状态事实源 |
| 3 · 阶段 0 | 固定行为样本、验证命令和实施范围，完成适用独立设计复核；随后准备阶段 1 自身准入 | B02 收敛；阶段 0 设计结论和阶段 1 准入分别可判断，能复用相同设计证据而不重复整套审查 |
| 4 · 阶段 1 | 先落地验收闭环与风险分流（F01—F03、R01），同步必要的计划/机器判定；规则直接写入目标参考文件 | 低风险自验、高影响独立、技术完成待用户验收的行为一致；新增能力随改随验 |
| 5 · 阶段 1 | 在同一规则来源落实证据复用、增量复验、恢复读取、输出交接及 subagent 调度（R02—R04、E01—E04） | 可信证据可复用，变化/冲突触发适用补验，同一未修正阻塞不重复送审；等待不越界 |
| 6 · 阶段 1 | 先使参考资源和只读 CLI 入口可达，再精简 SKILL 与项目受管块，完成分发和旧项目兼容验证 | skill-only、CLI-only、版本差异和缺依赖均行为明确；受管块外字节和既有文档保持 |
| 7 · 阶段 1 | 完成代表性任务对照（E05）、受影响回归及必要独立完成复核，交用户体验验收 | 质量反例仍能检出；可说明读取/复核/时长变化及局限；用户接受后才关闭本计划 |

第 4—6 项可以在不影响受审对象的前提下准备互不依赖的材料；共享规则单一写入。不要先扩写长 SKILL 再整份搬家；三份参考从规则落地起就是目标位置。正式 CI/发布检查沿用前置契约，发布、安装同步和其他项目操作不包含在本计划默认实施授权中。

## 背景

用户要求将尚未合并的[技能审计与使用场景交接](../reviews/plan-governance-skill-usage-audit-20260906.md)转成独立后续计划。先完成 [iterative-governance-reliability](iterative-governance-reliability.md)，再对照其最终成果实施本计划中的剩余改进。原报告保留发现、用户反馈和历史样本；本计划是后续目标、行为差异和验收设计的事实源，状态与先后关系由 [PLAN_MAP](../PLAN_MAP.md) 维护。

历史记录（本次推进授权前）：2026-09-06 执行前整理时，前置阶段 0—4 已完成独立技术验收，见[最终报告](../reviews/iterative-governance-reliability-stage4-completion-review-20260906.md#第二轮通过)。本计划继续保持阶段 0 设计中；前置用户接受记录尚待补齐，不能将技术验收、提交请求或“一会开始执行”自动记为产品接受。本轮授权是整理文档，尚未开始实现。

2026-09-06 本次推进：用户明确要求“推进这个计划”，并澄清临时口头自验约定须落实为其他会话可读取的 skill 规范。这是本计划的推进/实施授权，不是追溯性的前置产品接受。B01 的交接要求据此收敛为已提交且独立技术验收的基线，以及当前显式推进授权；本计划自己的最终用户验收仍保留。

## 目标

- 减少无效追问、重复复核和无理由的全量验证，同时保留授权边界、缺陷发现与独立判断。
- 补齐可观察验收、相关性能基线、必要的早期体验、AI 自主修复和用户最终验收闭环。
- 精简 skill 入口及项目受管规则，按需读取详细说明，避免多处维护同一契约。
- 降低执行中的重复读取、证据交接和独立复核调度开销，用代表性任务观察上下文压力与实际完成时长。
- 只处理前置计划完成后仍存在的差距，以实际样本判断效果，不承诺未经测量的耗时改善比例。

## 非目标

- 不接管或重开前置计划；不重复实现其门禁修复、统一 verify/发布恢复、文档职责和可选证据绑定。
- 不取消现行门禁后再用新规则自我批准；不恢复已废弃的整计划自主执行运行时，不默认创建 goal 或调度服务。
- 不将所有生命周期阶段拆成独立 skills，不强制所有项目采用用户逐阶段签收，不增加固定文档组合或统一性能数值。
- 不为未安装 skill 和 CLI 的环境生成项目内完整治理规则副本；治理规则的读取以已安装 skill 或 CLI 为前提。
- 不迁移历史项目、不清理用户目录残留、不解除宿主回放阻塞；提交、发布、全局同步和 Docker/TunnelPad 实际操作另按授权执行。

## 需求探索

### 已确认事实与结论

- 用户通常提出功能，经 grilling 确认后持续实施；AI 先功能和运行验证，用户再查看、操作并观察 CPU、内存及磁盘读写；用户接受后关闭功能计划。
- 原范围小问题由 AI 自行修复、复验及必要的重新送审；新取舍、授权变化和无法自行解决的问题才升级。新点子或较大改动另开计划。
- 用户反馈复核时间占用较多，要求纳入原报告的 F01—F03 和 R01—R04；本次明确选择独立后续计划，不再合并到正在执行的计划。
- 用户于 2026-09-06 明确要求：后续简单改动先由当前 AI 自行复核，不每次走独立复核。用户随后澄清这不是仅当前会话的临时例外，须落实到共享 skill 供其他会话遵循；具体边界见[复核分流约定](#按风险选择自验或独立复核)；不把自验结果写成独立验收。
- 用户已确认安装前提：精简项目 AGENTS/CLAUDE 后，无需支持将项目复制到既未安装 skill、也未安装 CLI 的环境仍能读取完整规则；不新增项目内完整规则文档。
- 用户要求补入任务执行记录对照发现的五项遗漏，并反馈：自从独立复核接入 subagent，整个计划的执行时长感觉明显增加。这是已确认的使用反馈，尚无同等任务的前后对照或增幅测量，不能将全部耗时归因于 subagent。
- 已确认场景直接沿用，不重复完整 grilling。下列尚未确定的技术策略保留为候选，不把方案建议写成已完成的用户选择。

### 候选方案与取舍

推荐保留一个治理入口，按需拆出计划/契约、验证/复核及 CLI 操作参考，复用已有 grilling 和项目专业工具。入口保留任务分流、必要底线和明确读取条件；细节只维护一处。安装前提已确认，以下具体划分及 CLI 读取入口仍是待对照前置成果的设计提案，不代表已实施或整个设计已通过。

未定事项集中在[阶段 0 设计交付](#阶段-0-设计交付)，不把每个文件名或低影响实现细节升级给用户。优先复用现有枚举和记录区；确需改变公共字段时先明确差异及兼容策略。

### 入口精简与按需拆分提案

保留一个 `plan-governance` skill，不按生命周期另建多个 skills。下表路径相对于 `resources/skill/`；尚未创建参考文件。

| 内容 | 处理与拟议位置 | 读取条件 |
|---|---|---|
| 启用判定、任务分流、当前步骤和下一决策 | 合并到 `SKILL.md` 的简短入口，明确普通小改、已有计划内反馈和新计划的区别 | skill 被调用时 |
| 授权复用、不可自批失败、用户最终验收等必要底线 | `SKILL.md` 保留简短明确的约束，并链接适用细节 | 不依赖读完全部参考才能知道底线 |
| 文档职责、需求探索输出、状态/阶段、事实源与同步、历史草案 | 去重后放入 `references/planning.md` | 新建或调整计划、范围、契约、状态时 |
| Step 0、验证、独立复核、修复重审、证据复用、用户验收 | 放入 `references/verification.md`，给出适用条件和停止条件 | 准备阶段证据、验证、复核或处理验收反馈时；只读适用部分 |
| 初始化、升级、漂移检查、证据绑定、图谱及 hook 操作 | 从入口移至 `references/cli.md`，说明已存在的公开命令和资源查找方式 | 确实需要相应 CLI 操作时 |
| 计划、ADR、migration、spec 等模板 | 保留 `assets/`，引用现有模板，不把模板正文复制到入口 | 确需创建对应文档时 |

grilling 继续复用已有技能；治理入口只负责何时调用、已确认内容何时可跳过及应留下哪些输出。普通小改不要求读完三份参考，也不强制建计划；已有计划覆盖时按受影响内容更新。拆文件本身不改变复核时点，复核减负仍须落实 R01—R04。

### 安装前提与项目入口

项目 AGENTS/CLAUDE 的受管块拟只保留治理启用说明、规则读取方式、项目事实源路径和少量必要底线；项目特有约束仍在项目中维护。通用细节由安装资源提供，不再生成另一份完整项目规则。迁移须保留受管块之外的用户内容，继续遵守现有仅更新 agent 规则和升级不覆盖项目文档的边界。

- 已安装 skill：从宿主发现的 skill 入口读取，并按需读取该安装包内的参考和模板，不硬编码 `~/.agents` 等安装路径。
- 仅安装 CLI：需要补充公开的只读规则读取入口。当前 CLI 尚无该能力；候选为 `plan-governance-cli guide [topic]`，先返回简短入口，再按主题返回包内参考。命令名尚未冻结，设计要求是不依赖当前工作目录、不下载、不安装、不写项目文件；它不是现有命令，也不能以 `setup` 代替只读查询。
- 两者都安装：一次治理工作沿用同一份已选规则来源及其参考，不混读不同安装版本的正文；实际调用 CLI 时核对所需能力，遇到相关契约不兼容再处理，不为所有版本差异增加审批。
- 两者都未安装：明确指出治理规则读取的安装前提缺失，不自动安装、不假定已遵循不可得规则，也不因此阻塞无关的普通工作。

“skill 或 CLI”是规则可发现性的前提，不意味着只有 skill 就能运行 CLI 命令；当前阶段若需要实际 CLI 检查，仍需相应可执行工具。参考文件及 CLI 读取内容应复用同一份包资源，并纳入分发清单验证，不另维护重复正文。

## 与前置及相关计划的边界

| 已有或正在完成的能力 | 本计划如何承接 |
|---|---|
| 前置阶段 1：准入诊断、阻塞和工作集修复 | 作为回归基线保留，不重写检查器；候选减负规则若影响判定，单独设计兼容后再纳入实施 |
| 前置阶段 2：统一 verify 与发布失败恢复 | 保持强制 CI/发布检查，证据复用不用于跳过这些检查 |
| 前置阶段 3：契约职责、小任务分流、场景表、模板及字节保护 | 对照最终成果去重；补齐验收时机、最终用户接受、复核减负及入口结构，不重新开发已完成能力 |
| 前置阶段 4：可选 attestation 范围绑定 | 复用最终机制，不另建快照引擎；文件 hash 不能单独证明测试环境、范围完整或独立验收有效 |
| [phase-local-review-dispatch](phase-local-review-dispatch.md) | 仅协调共同复核契约和共享文件；宿主回放及其阻塞继续由原计划负责，不把宿主阶段完成作为本计划硬依赖 |
| [requirements-grilling-integration](requirements-grilling-integration.md)、[independent-acceptance-rules](independent-acceptance-rules.md) | 复用已确认探索退出和验收独立性，修复入口遗漏；不复制其历史全文或改写旧完成记录 |

前置已覆盖三类任务分流、可观察场景表、按需契约、受管块外字节保护和可选范围绑定，直接引用其验证，不重复开发。按本计划扩展目标判断：F01/F03/R01/R02/R03/E01—E04 为部分覆盖，F02/R04/E05 尚未覆盖；部分覆盖不等于整项完成。

本次只修改本计划、交接报告转接说明和地图相关条目。项目简单改动自验约定已由前置任务写在 AGENTS/CLAUDE 受管块外，本次保留。共享实现待交接、准入和实施授权齐全后再由本计划单一写入；不接管宿主回放或其他任务的提交、发布。

## 公共契约变化

用户已授权将风险分流落实到通用 skill/生成规则/检查器，以下为实施及验证目标；设计准入前尚未替代现行机器契约。来源编号用于追溯原报告；此后的具体规则以本计划收敛结果为准。

| 来源 | 拟议差异与验收要点 |
|---|---|
| F01 | grilling 收敛时固定少量场景、输入/前置、操作和可观察结果；相关性能指标与可比环境、负载、窗口一起确定，不发明通用阈值 |
| F02 | 交互或方案明显不确定时先交付关键流程供体验；明确小改不增加中途签收，不把未回复当作接受，可继续不依赖反馈的工作 |
| F03 | 技术验证提供可复验命令/步骤及实际结果；原范围小缺陷自主修复、复验；采用该协作方式的功能计划经用户接受后关闭，技术完成不冒充用户接受 |
| R01 | 用户已确认简单、低风险且边界、影响与验证明确的改动由当前 AI 自验，不逐次独立复核；高影响变更保留独立复核；独立复核时点及必要例外按下文风险分流候选收敛，不按代码行数判断风险；现行阶段规则变更先明确兼容 |
| R02 | 普通自验失败由当前 AI 修复、复验；已经触发独立复核的缺陷重审聚焦原阻塞、修复差异及受影响回归，引用仍有效的原证据；失败记录保留，由独立复核者追加新结论，不改分类自批 |
| R03 | 可信全量证据覆盖当前受审工作树、依赖、配置、环境和验证契约时可复用，复核者核对并复验关键风险点；相关变化、缺失或冲突时补验，版本号或 HEAD 相同不够 |
| R04 | 同范围行为后测与完成复核适用时合并；复核者未参与实施或测试编写，先盲测再对照预期，行为与机械结果分别记录 |
| 初次审计：入口与资源 | 精简 SKILL.md 和受管 AGENTS/CLAUDE，详细手册按需加载；明确 npm CLI 与 skill 资源位置，参考文件和模板随分发可达，不自动删除安装目录残留 |
| 初次审计：追问、授权和文档验证 | 补齐已确认方案跳过、非阻塞追问退出和既有授权复用；纯文档执行适用检查并说明代码覆盖率不适用，不为字段创建无价值测试 |

### 按风险选择自验或独立复核

2026-09-06 用户已确认：后续简单改动先由当前 AI 自行复核，不每次走独立复核。随后用户明确要求后续会话使用 skill 时也遵循统一规范。本轮将约定落地到共享 skill、模板、生成器和适用机器判定；现有 AGENTS/CLAUDE 中临时项目说明在共享规则生效后移除。本轮不安装或同步用户目录中的已分发副本。下表是共享规则的风险边界。自验、独立复核和用户产品验收分别记录，不能把自验结果写成独立验收通过。

| 情况 | 推荐处理 | 适用边界与例子 |
|---|---|---|
| 低风险，目标和影响范围明确，易撤销且实际验证足够 | 当前 AI 检查差异、执行适用测试/运行验证，修复并复验；简要记录结果，不默认派独立 subagent 或新建复核报告 | 提示文案、局部 UI 调整、已定位且影响有限的小 bug；普通功能若满足同样条件也可自验，是否进入计划本身不决定独立复核 |
| 高影响变更 | 独立复核；在关键设计决策前或完成时安排必要检查，按风险选时点，不给每个内部步骤单独派发 | 安全/权限/公开暴露、数据完整性或迁移、核心公共契约和生命周期、较大范围共享逻辑、难以撤销的操作。少量代码或一行配置也可能属于高影响 |
| 暂时无法判定风险、反复修复无效或证据冲突 | 先由当前 AI 补事实和定向验证；仍存在实质不确定或盲点时，说明原因并请求定向独立检查 | 不把每个未知细节都升级成全量复核，也不凭自信判为低风险；用户明确要求独立复核时保留该要求 |

可逆性同时评估实际外部后果，不以代码可回滚替代操作可撤销。执行中发现影响超出原分类时重新评估，不能等自验结束后仍按旧范围放行。已有独立失败结论或未解除的有效阻塞不因改分类而消失。用户最终查看、操作和相关性能验收保持原约定；普通小问题仍自主闭环，高影响动作是否需要用户授权继续按既有授权边界判断。

新策略下阶段转换或计划收尾本身不自动触发独立复核，是否需要由实际风险及适用契约决定。阶段 0 须明确低风险自验在计划、check/workset 和生成规则中的一致表达，以及现有强制独立准入记录的兼容方式；不得用“自验通过”伪填“独立复核通过”。本计划对跨项目准入/复核保障的规则修改本身按高影响变更处理，不能使用拟议豁免批准自身；正在执行的前置计划继续遵守其原门禁。

## 执行减负与复核调度

以下补充细化已有 F/R 目标，仍属待收敛的目标行为，不立即改变正在执行计划的门禁。实现落在既有单入口及三份参考中，不新增阶段、独立 skill、强制计时表或调度系统。

| 补充项 | 目标行为与边界 | 拟归属 |
|---|---|---|
| E01 恢复与按需读取 | 继续任务或上下文压缩后，先从现有工作集、当前阶段和最近证据定位目标、授权、阻塞及下一动作，并核对实际工作树。历史和代码按缺口读取；已有材料不足或变化时扩大读取，不机械重读全计划/全库。复用现有记录，不另建第二份状态事实源 | planning |
| E02 输出与证据交接 | 主对话和复核回传优先给出结论、有效阻塞、实际命令、受审范围/环境及证据入口。需要跨阶段或跨任务复用的关键输出须可被接手者访问；会话内输出 ID 只有在检索方式可用时才能作直接入口，文件 hash 不代替实际结果。详细输出按需读，不逐项新建报告或反复贴全文；缺证据时补证或复验 | verification、cli |
| E03 复核范围与结束条件 | 在 R01—R04 上增加前序回归选取：按实际差异、共享调用和契约风险选择，未受影响且证据有效的部分引用旧结果。送审前固定对象；通过后仅追加结果/同步状态时做适用文档检查，实质变更、证据失效或新缺陷才触发相应补验。不得因“只是文档”忽略契约、授权或完成条件的变化；仍遵守适用的快照失效和 CI/发布检查 | verification |
| E04 并行变化与诊断归属 | 明确当前命令使用仓库实现还是已安装 CLI、目标版本及证明范围；区分本次引入、既有问题和并行写入。无关问题不使当前行为测试结果自动失效；修复责任及适用补验范围清楚后处理，不自动接管其他计划。不隐去全仓失败、不把局部通过当作全仓通过或绕过正式门禁 | planning、cli |
| E05 减负效果验收 | 对少量可比任务观察重复读取、加载量、全量验证次数、复核轮次和实际完成时长；同时验证缺陷发现、授权及用户验收正确性。仅在宿主提供可靠统计时引用 token，文件大小、压缩次数和工具次数不能冒充 token 占比或浪费比例 | verification |

### Subagent 复核时长与调度

独立复核可能增加串行等待：实施者自验后，复核者还需要获取上下文、检查风险、执行必要验证并返回结论。它可减少等待用户逐项安排的停顿，但不是天然的加速保证。前置阶段 4 的全量测试通过后，独立实盘仍发现 [D02/D03](../reviews/iterative-governance-reliability-stage4-completion-review-20260906.md#第一轮未通过)；此类检查及必要修复保留，减负重点是重复准备、重跑和交接。

- 先按 R01 判断是否需要独立复核；仅在需要时，拟默认由一位独立复核负责人统筹该复核点。专门子复核仅用于明确、可分的风险，划清范围，合并可共用的机械检查，不为每个内部步骤新增复核。专家的有效阻塞不得被汇总结果或多数通过覆盖；实现者及测试作者不能充当自己的独立验收者。
- 派发时提供当前范围、契约、变化、关键风险和 E02 证据入口，使复核者能够自行查证；不发送完整聊天历史替代任务说明。需要行为盲测时，预期答案按 R04 隔离，紧凑交接不能破坏独立判断。
- 等待复核时可推进已授权、无需该结论的准备工作；不得越过门禁，也不得改动受审对象及其依赖、配置、环境、测试数据。若并行变化影响证据，按实际范围标记失效并补验。
- 同一未修正阻塞不重复派发求通过；修复后进行适用的独立增量重审，影响扩大则扩大复验。等待优先使用宿主的有界等待/状态变化机制，减少无变化轮询和重复追问；不可用、超时或失败仍保留阻塞，不因耗时自动通过。
- 效果对照区分必要审查/缺陷修复、重新读取/重复验证、派发/等待/交接，以及用户等待、额度中断等非执行时间；关注决定任务何时结束的串行环节，并行子任务的耗时不能直接相加冒充总时长。任务规模和风险不同的历史案例仅作线索，不据此宣称固定加速比例，也不为节省时间降低完成条件。

## 不变量

- 原授权持续有效，范围和高影响边界变化时才重新确认；独立复核不能代替用户授权。
- 失败、不可用或冲突不得自行批准；允许授权范围内补证据和修复，未解除前不越过适用门禁。
- 变更风险决定检查深度；可选风格建议不自动成为阻塞，不为每个样本、微小修复或报告另造阶段门。
- 当前阶段写细、后续阶段写粗；新增规则生效前遵守现行准入，不用拟议减负规则放行自身。
- 单一事实源和单一写入边界保持；状态、关系、阻塞及证据链接同步地图，历史记录不覆盖。

## 影响模块或文件

阶段 0 文档及证据范围：

- `docs/plans/plan-governance-workflow-streamlining.md`
- `docs/reviews/plan-governance-skill-usage-audit-20260906.md`：仅同步转接入口，不改历史发现。
- `docs/fixtures/plan-governance-workflow-streamlining-cases.md` 及本计划独立复核报告。
- `docs/PLAN_MAP.md`：本计划索引、推荐顺序、关系和阻塞条目。

阶段 1 实施范围（具体差异限于已收敛契约）：

- `resources/skill/`
- `resources/manifest.json`
- `bin/plan-governance-cli.mjs`
- `scripts/init_plan_governance.py`
- `scripts/check_plan_governance.py`
- `tests/test_risk_review.py`
- `tests/test_workset_evidence_limit.py`
- `tests/test_check_plan_governance.py`
- `tests/test_plan_governance_hooks.py`
- `tests/test_init_plan_governance.py`
- `tests/npm_cli.test.mjs`
- `package.json`（后续已授权的 1.0.0 发布元数据）
- `package-lock.json`（后续已授权的 1.0.0 发布元数据）
- `README.md`
- `AGENTS.md`
- `CLAUDE.md`


hook 通过共享检查器得到相同判定，不另外实现风险解析；仅在验证发现适配必要时修改 hook，并先记录依据。保留发布脚本、宿主调度、图谱运行时及旧快照。阶段 1 实施原范围不含发布；用户于技术交付后已明确授权 `1.0.0` 发布和本地更新，由分发维护记录承接，不据此宣称实际减负效果已验收。

## 阶段路线图

| 阶段 | 目标 | 进入条件 | 验证方向 | 状态 |
|---|---|---|---|---|
| 阶段 0 | 对照前置最终成果、收敛剩余行为和兼容设计 | 已提交前置基线与推进授权 | 差距、契约和样本独立设计复核通过 | 已完成 |
| 阶段 1 | 实施剩余闭环与减负规则，精简入口并完成用户验收 | 本阶段自身基线、样本和独立准入通过，实施已授权 | M01—M07、C01—C04、独立完成复核及最终用户验收 | 实施中 |

## 当前阶段

### 范围

当前阶段 1：实施上列共享资源、风险判定及分发入口，并完成适用验证。阶段 0 设计与阶段 1 自身准入见[独立报告](../reviews/plan-governance-workflow-streamlining-design-review-20260906.md)；以下设计交付保留为本阶段契约。

### 阶段 0 设计交付

| 交付 | 推荐设计方向及必须固定的内容 | 必须验证的反例 |
|---|---|---|
| D1 风险复核与机器准入一致 | 以 R01 为默认分流；确定低风险自验的真实记录位置，及 check、workset、hook、模板和生成规则如何一致解释。优先向后兼容；若需新增可选字段，明确取值、缺省旧行为、非法值和默认/严格诊断，不修改历史记录 | 低风险自验不被迫伪填独立通过；高影响、已有失败和相关证据失效不能降级放行；未知声明不静默当低风险 |
| D2 技术完成与用户接受 | 优先沿用计划级“实施中”，在现有验证记录注明技术完成、等待用户验收；在适用完成条件和记录中保留用户接受证据，无须先增加状态枚举。原范围反馈回到修复/复验，新需求另行归属 | 独立技术通过、用户沉默、提交请求均不冒充产品接受；不让所有小任务增加逐次签收 |
| D3 规则读取、证据入口与分发兼容 | 收敛一个 skill / 三参考的职责；确定只读 CLI 子命令、主题、资源定位、退出语义和缺资源行为；复用包内正文。固定旧项目受管块迁移和必要证据的可访问方式，不建设新证据服务 | CLI-only 在任意 cwd 可读；未知主题/缺资源不成功返回空规则；旧命令保持；没有安装前提时不自动安装；不同版本不混读 |

这三项是设计交付，不另立三个子计划。范围内技术细节由执行 AI 查证后决定；影响授权、产品取舍或公共兼容承诺的变化再交用户确认。设计结论直接更新本节及相应契约，不追加多套并存建议。

### 已收敛的实施契约

D1 采用显式、向后兼容的风险策略：

- 当前阶段摘要可选字段 `复核策略`，唯一启用值为 `风险分流`；字段缺省完全沿用旧独立准入契约。空值、未知值、重复字段或新旧结构歧义不能默认低风险。
- 启用后，摘要以 `最新阶段复核` 链接替代 `最新独立准入复核`。固定章节 `最新阶段复核` 包含 `日期、阶段、方式、风险、风险依据、结论、证据、复核者`；`方式` 为 `自验/独立`，`风险` 为 `低风险/高影响/待判断`。通过必须有有效日期、当前阶段、非占位证据/依据/身份；自验只可用于低风险，待判断不可通过。风险判断覆盖范围、实际后果、可逆性及验证充分性，机器只验证声明结构，不推断真实风险。
- 结论按明确前缀声明结果：以 `通过 / 未通过` 等未选择候选开头时，无论其后标点或待选说明是什么，都视为未知；明确通过后的修复说明不受影响；明确失败前缀优先；明确 `通过` 前缀支持常见句末符号（包括 `通过。`）；其后的修复说明可提及历史冲突/超时，不因此重判失败；没有通过前缀的不可用/超时/证据冲突等声明仍失败。结构化有效阻塞独立保留，不能由通过说明覆盖；机器不推断自由说明真实性。
- `阶段复核记录` 是追加式八列表：`日期、类型、阶段、方式、风险、结论、证据、复核者`。最新当前阶段记录与最新表的日期、方式、风险、结论、证据和身份一致；失败/不可用/超时/证据冲突不算通过，修复另追加结果。普通无计划小改只简报自验，不建立这些表。
- 旧独立记录不迁移、不删除。显式启用新策略时，当前阶段仍有旧独立失败、未解除的历史失败或最新/历史冲突，仍阻塞；补充适格独立通过后更新旧最新结论并保留历史，才可解除。新策略内最后一次独立记录失败也不能被后续自验遮蔽。上一阶段历史不自动封锁当前阶段。
- check、workset 和 hook 共用判定；保留 JSON schema 1、键及动作枚举。有效低风险待自验用 `design/verify`，有效复核通过但地图仍设计中用 `design/sync`；高影响待独立复核用 `independent_review`。显式失败用 `blocked/resolve_blocker`，歧义用 `unknown/unknown`。默认 WARNING、活跃计划严格 ERROR 的既有分层保持。
- 新模板默认声明风险分流，但风险与复核结果保持待判断/未进行，绝不生成已通过。旧项目升级只更新受管入口，不自动迁移计划。旧 CLI 不理解新策略时可能拒绝新计划的严格准入，必须显式更新所用 CLI；不把旧版误报伪装成新能力。
- 不扩展 attestation 引擎或 hook 权限。明确记录的证据失效继续作为阻塞；实际内容/环境/依赖变化由验证规则及适用的 `check --check-attestations --strict-readiness` 核对，不能声称 workset 自动检测文件 hash 或环境漂移。

D2 不增加生命周期或 JSON 枚举。技术完成待用户验收的适用功能计划保持 `实施中`；在最近实施/验证记录放入技术结果、证据和待验收项，当前阶段显式 `下一动作：等待用户验收`。workset 将此映射为已有 `none`，reason 明确等待用户验收且不自动关闭。原范围反馈自主修复/复验，新需求重新归属；用户沉默、提交和技术通过均不补写接受。纯机械文档同步或普通小改不增加逐次签收。

D3 冻结规则读取与分发：

- `plan-governance-cli guide [overview|planning|verification|cli]`，缺省 overview，分别读取包内 SKILL 和三个 references 正文；`guide --help` 输出主题及用法。成功退出 0，未知主题、多余参数、缺失/不可读/空资源退出 1，stderr 给明确诊断；不启动 Python、下载、写项目或回退其他版本。
- 主题是封闭映射，按包根定位，与 cwd 无关。stdout 是所选 Markdown；入口解释 CLI 相对引用通过相应 guide 主题读取，skill 模式按实际发现的安装目录读取同源 references，不硬编码用户目录。不混用不同版本；包版本号相同不是资源相同的证明。
- 三参考进入 manifest/setup 与 npm 包；项目 AGENTS/CLAUDE 受管块仅保留加载方式、任务/权限底线及当前阶段约束。先提供可读资源，再精简入口。升级保持受管块外字节及旧文档；只删除本仓库本轮明确替代的临时项目自验说明，不通用清理用户内容。

阶段 1 采用与阶段 0 同一份冻结契约和[执行样本](../fixtures/plan-governance-workflow-streamlining-cases.md)，自身 Step 0 是 HEAD `7cd6953` 的行为/资源基线，范围为上列源、资源、测试及同步文档。独立审查分别判断阶段 0 设计完成和阶段 1 是否达到待实施标准；不以阶段 0 通过自动放行阶段 1。

### 阶段 1 自身准入材料

- 目标/范围：交付本计划 F01—F03、R01—R04、E01—E05 的剩余差距及已收敛 D1—D3；文件以影响范围为准。前置已覆盖部分保持，不重复实施。非目标沿用本计划已列边界。
- Step 0：接手提交 `7cd6953` 的源与安装差异、旧独立硬门禁、缺少 references/guide 的实测结果，以及执行样本 M01—M07/C01—C04 输入；这是行为/契约替代基线，不是新功能通过证据。
- 验证：先 M02—M06 受影响测试及真实临时 CLI 链路，完成后统一 M07；C01—C04 对照必要动作和证据复用，记录可取得的读入量/复核/全量调用观察及其局限。行为检查者未参与规则实现，先取得输入再对照预期；结果可与独立完成复核合并。
- 技术完成条件：共享规则覆盖各 F/R/E 目标；低风险/高影响/失败重审与机器判定一致；四主题及所有参考在源和临时安装包均可读取；受管块外和旧计划保持；适用回归与全量验证通过；代表性行为无未解决的授权/风险/验收错误，剩余效果测量限制如实记录；独立完成复核通过。
- 全计划完成条件：技术条件齐全后，交付可审阅的资源和结果，记录本次功能计划的用户接受，再将地图标为已完成。技术完成等待期间保持实施中，普通落档不新增签收。
- 失败/回滚：反例错误放行、资源读取缺失、旧契约回退或用户字节被改均阻塞完成；在授权范围内定向修复及复验，独立失败由独立者增量重审。只回退本计划具体差异，保持旧策略可用，不发布、改真实资源或覆盖用户安装；本计划不以新低风险策略批准自身。
- 准入结果：独立审查已分别确认阶段 0 完成和阶段 1 达到待实施标准，见最新独立准入复核；阶段 1 开始实施。

### 阶段准入摘要

| 字段 | 内容 |
|---|---|
| 准入状态 | 实施中 |
| 复核策略 | 单次独立复核 |
| Step 0 | [当前只读基线与历史快照](#step-0-证据)；执行前仍核对交接后的工作树 |
| 样本矩阵 | [执行与修复样本](../fixtures/plan-governance-workflow-streamlining-cases.md)；历史 U01—U19 仅作输入来源 |
| 验证方式 | [文档及后续设计验证](#验证方式) |
| 失败/回滚边界 | [风险和回滚](#风险和回滚) |
| 当前阻塞项 | 无 |
| 最新阶段复核 | [当前单次复核与修复自验](#最新阶段复核) |
| 下一动作 | 等待用户验收 |

### Step 0 证据

基线类型：当前工作树只读文本与历史运行记录，非功能或性能通过证据。2026-09-06 立项读取 HEAD `336b728d7dacc335a35f2cf97ab39356e3a82ba0`，工作区存在其他任务的未提交变更，HEAD 不代表完整受审身份。

| 对象 | 立项观察 | SHA-256 |
|---|---|---|
| 仓库 `resources/skill/SKILL.md` | 350 行、23526 字节；包含流程和完整操作手册 | `12e270717d235315363893881b7b4c75afc65961b02cdb80b46a4124fa194ec0` |
| 已安装 `~/.agents/skills/plan-governance/SKILL.md` | 332 行、21460 字节，与当前仓库资源不同；本轮不更新 | `a9538a355fb556a3d99f88072967227259120d6f8c95c9b726c3a996cf7a2e97` |
| 仓库计划模板 | 已有用户可观察验收表；需确认最终用户关闭和性能约定的剩余差距 | `af5e5719b8df8c671d3eb0ea6ab5713a24b559464a19fc0c47ff13311dbdf907` |

上述保留为立项历史。2026-09-06 本次整理重新读取：仓库 SKILL 为 352 行、24378 字节，SHA-256 `d99df9cd4173416fcbd92c06ae7050a8c08ef0d79e009e1f0435c4ecfa3fd3ae`；计划模板指纹仍与上表一致。HEAD 当时未变，但工作树已有前置交付和并行文档修改；尚无 `references/`，manifest 未列参考文件，公开 CLI 尚无只读规则入口。前置验收报告可访问，其历史测试结果仅引用、不在本轮重跑。实施接手时仍核对最终工作树、未提交差异及调用的是仓库还是安装版本。

2026-09-06 执行观察补充：读取任务“优化项目治理规范”（`01a071cc-a15f-7f42-8d94-4f668eb6194e`）截至当次读取的记录，发现 3 次上下文压缩及恢复后成批读取、重叠代码区段读取；这不是耗时或 token 因果统计。[阶段 3 报告](../reviews/iterative-governance-reliability-stage3-completion-review-20260906.md#实际命令和证据)明确工具输出 ID 属于复核会话；[阶段 4 报告](../reviews/iterative-governance-reliability-stage4-completion-review-20260906.md#其余验证与局限)记录前序子复核和全量通过后仍存在的真实缺陷。[前置计划](iterative-governance-reliability.md)的“并行文档兼容”记录说明后续计划四个状态单元格曾干扰严格检查且已修正；这里只保留历史原因，最新状态以原计划和地图为准。

2026-09-06 实施交接基线：HEAD `7cd6953`，首次 `git status --short` 输出为空；仓库 skill 与检查器分别为 `d99df9cd4173416fcbd92c06ae7050a8c08ef0d79e009e1f0435c4ecfa3fd3ae`、`669c6c3e7e0690f5e57c98a4a221af7a712a1ff236a6cf550a765384e77c6ebb`。仓库与安装 CLI 均显示 0.3.5，但资源不同，后续验证统一使用仓库入口；安装副本仅作旧版对照。以下历史矩阵保留为来源，具体实施输入、命令和完成条件由[执行样本](../fixtures/plan-governance-workflow-streamlining-cases.md)固定。

### 样本矩阵

阶段 0 使用只读命令及设计走读；结果追加到本计划验证记录，不生成测试或构建产物。阶段 0 先选定至少四组同完成条件的对照任务：低风险局部修复、普通功能含反馈、单行高影响配置、独立失败后的修复重审；记录任务输入、旧规则版本、环境、证据入口及可取得的计时/读取指标。历史案例不可比时只作线索，阶段 1 获准后在相同输入上演练旧/新规则。阶段 1 从下表选取最少代表性输入并固化可执行入口；当前“尚未执行”条目不能当作准入证据。

| 输入或基线 | 命令/检查方式 | 预期结果与失败判定 | 输出位置 |
|---|---|---|---|
| 前置阶段 1—4 最终成果与本计划目标 | `rg -n '行为契约|用户可观察验收|完成条件' docs/plans/iterative-governance-reliability.md`；逐条对照目标差异 | 已覆盖项可追溯，保留真正差距；将前置未完成内容当作已验收即失败 | 命令输出；本计划剩余差距记录 |
| 源 skill、模板和受管规则 | `rg -n 'grilling|独立复核|用户.*验收|覆盖率|scripts/|references/' resources/skill/SKILL.md resources/skill/assets/plan.template.md AGENTS.md CLAUDE.md` | 定位重复、冲突和条件读取缺口；仅凭行数认定性能改进即失败 | 命令输出；本计划设计记录 |
| 小修复、普通功能、等待用户接受、高影响变更 | 按[历史 U01—U19 输入](../reviews/plan-governance-skill-usage-audit-20260906.md#拟补充的验收样本)分组，对照本计划拟议差异；阶段 0 仅设计，后续盲测不提供预期答案 | 明确必要动作及停止边界；将所有输入统一全流程、提前泄漏预期或混淆授权即失败 | 本计划精简后的执行矩阵，尚未执行 |
| 局部低风险修复、一行高影响配置、影响扩大、已有独立失败 | 走读[风险分流候选](#按风险选择自验或独立复核)，阶段 1 再做实际行为样本 | 低风险自验在文档及 check/workset/hook 中一致且不伪称独立通过；高影响/扩大的风险及时升级；以行数分档、仍为每个低风险节点派发或重分类自批旧失败即失败 | 本计划风险矩阵，尚未执行 |
| 证据复用与失效反例 | 对照前置证据绑定边界，设计源码/环境/依赖/验收契约变化及未变化对照 | 变化能触发适用补验，未变化可复用可信证据；只看版本号或 hash 即失败 | 本计划兼容设计，尚未执行 |
| skill 可用、仅 CLI 可用、两者均不可用及版本不一致 | 走读[安装前提与项目入口](#安装前提与项目入口)；阶段 1 再验证真实资源读取与分发 | 可用环境按需找到同一来源规则；缺少前提能明确报告；不存在的命令被当成已有能力、自动安装或额外生成完整项目规则即失败 | 本计划分发设计，运行验证尚未执行 |
| 恢复上下文、关键输出过长或证据入口不可访问 | 按 E01/E02 设计相同任务的继续执行与新复核者接手样本 | 从现有当前记录恢复并核对变化，关键输出可访问；整套重读、仅凭不可检索 ID 宣称复用或遗漏真实失败即失败 | 本计划行为矩阵；后续演练尚未执行 |
| 前序代码未变/共享调用已变、仅落档/契约变化、并行计划检查失败 | 按 E03/E04 对照三组正反场景；分别标明局部与全仓结果 | 补验范围随影响变化，缺陷归属清楚；把真实失效当成可复用、掩盖全仓失败或无理由重跑全部历史即失败 | 本计划行为矩阵；后续演练尚未执行 |
| 普通复核、专项风险、失败修复、等待期间依赖变化 | 走读 subagent 调度边界，设计与旧流程可比的代表性任务 | 复核分工不重复，有效阻塞保留，变化触发补验；为减时自批或混淆并行工时和总时长即失败 | 本计划 E05 效果对照；尚未执行 |

### 验证方式

- 阶段 0 检查设计、执行样本、地图登记和报告转接：本地链接/锚点、固定结构、关系、反向引用、空白格式及 `PYTHONDONTWRITEBYTECODE=1 plan-governance-cli check .`。普通检查不替代独立准入。
- 阶段 0 完成前，以前置最终成果收敛可执行行为矩阵和证据复用条件；明确变更后的检查器是否需要兼容调整，再提交适用独立准入。不得以历史报告或本轮文档检查替代。
- 阶段 1 使用执行样本已固定的命令：优先行为样本与受影响测试，分发变更验证资源可达和原文保护；既有 CI/发布要求保持。复核理由、复用依据及实际补验范围需可解释，不要求每个小任务填报计时表。
- 效果验收按 E01—E05 和 subagent 调度边界选择少量可比任务，记录相同完成条件、任务规模/风险、工具/环境及实际观察；既观察重复开销，也保留已知反例检出。没有可靠数据时报告局限，不以文件变短、调用变少或更早停止替代质量与耗时结论。
- 代码覆盖率不适用：阶段 0 无代码或测试变更，实际验证是文档和只读基线；阶段 1 按实际实现记录适用覆盖证据。

### 测试覆盖率

1.0.0 发布前阶段 1 修复后完整 verify：585 个 Python 测试通过，覆盖率 93.55%（门槛 85%）；101 个 Node 测试通过、0 skipped。详情与受测身份见[最终集成验证](../fixtures/plan-governance-workflow-streamlining-cases.md#最终集成验证)。后续限量输出的当前验证见[增量结果](#workset-最近证据限量输出)；历史结果保留。

### 完成条件

按[阶段 1 自身准入材料](#阶段-1-自身准入材料)的技术及全计划完成条件逐项验收；技术通过后仍须本计划用户接受，不自动关闭。阶段 0 的设计完成结论保存在独立报告。

### 阶段证据

- `docs/reviews/plan-governance-workflow-streamlining-completion-review-20260906.md`
- `docs/reviews/plan-governance-workflow-streamlining-design-review-20260906.md`
- `docs/fixtures/plan-governance-workflow-streamlining-cases.md`
- `docs/reviews/plan-governance-skill-usage-audit-20260906.md`
- `docs/plans/plan-governance-workflow-streamlining.md`

### 最近实施/验证记录

| 日期 | 类型 | 动作/结果 | 证据 | 状态 | 记录者 |
|---|---|---|---|---|---|
| 2026-09-06 | 用户立项与只读预对照 | 用户要求独立后续计划；读取前置阶段 3—4、当前规则与模板并记录快照 | 本计划 Step 0、原交接报告 | 设计中；尚未实施或准入 | Codex |
| 2026-09-06 | 立项文档验证 | 新计划/报告及地图新增引用共 30 个本地链接和锚点通过；固定章节、反向引用和空白格式通过；全局 CLI 普通及 strict-readiness 均退出 0 | 本轮命令标准输出；下方告警说明 | 文档检查通过，不构成阶段准入 | Codex |
| 2026-09-06 | 文档一致性核对 | 新计划、地图和报告的授权、前置关系、去重及目标覆盖无阻塞性文字问题；未改文件或运行测试 | 本轮只读子代理结论 | 非实施准入，B01/B02 仍保留 | /root/skill_instruction_audit |
| 2026-09-06 | 安装前提确认与结构提案 | 用户确认依赖已安装 skill 或 CLI，不新增项目内完整规则；记录单入口、三份按需参考和仅 CLI 环境的只读读取提案 | 本计划需求探索；用户对安装前提问题的回复 | 安装前提已确认；具体设计仍待收敛，未实施 | Codex |
| 2026-09-06 | 结构提案文档验证 | 新计划、报告及地图本计划引用共 32 个本地链接和锚点通过；固定章节、反向引用及空白检查通过；全局 CLI 普通检查退出 0，保留原有 11 条告警 | 本轮命令标准输出；下方告警说明 | 文档检查通过，B01/B02 与设计中状态不变 | Codex |
| 2026-09-06 | 执行减负补充 | 用户要求补入五项遗漏，并反馈引入独立复核 subagent 后总执行时长明显增加；记录 E01—E05、调度边界及效果对照样本 | 本计划执行减负与复核调度、Step 0 补充观察 | 需求和提案已记录；未实施、未证明提速 | Codex |
| 2026-09-06 | 补充文档验证 | 37 个本地链接/锚点、固定标题、反向引用及空白检查通过；已安装 CLI 与仓库 CLI 的普通全仓检查均退出 1，错误归属前置计划仍有 D02/D03 当前阻塞；未改变其结论 | 本轮命令输出；前置未决问题 | 本次文档检查通过，全仓检查未通过；本计划仍设计中 | Codex |
| 2026-09-06 | 复核风险分流候选 | 用户提出简单任务当前 AI 自验、高影响独立复核；补充 R01/R02、风险反例、动态升级和现有门禁兼容要求 | 本计划按风险选择自验或独立复核 | 推荐方案已记录；本次未修改现行规则或前置结论 | Codex |
| 2026-09-06 | 风险分流文档验证 | 39 个本地链接/锚点、固定标题、反向引用和空白检查通过；当次已安装 CLI 普通全仓检查退出 0，保留 11 条共享目标/背景引用告警 | 本轮命令输出；独立通过和实际新行为均尚未验证 | 文档检查通过，设计中状态保留 | Codex |
| 2026-09-06 | 用户确认简单改动自验 | 用户明确后续简单改动由当前 AI 自行复核，不逐次独立复核；本项目约定落入 AGENTS/CLAUDE 受管块外，通用分发与机器准入仍待设计 | 本次用户指令、复核分流约定；差异/引用自查、git diff --check、已安装 CLI 普通检查及仓库 CLI 严格检查通过 | 本项目协作约定生效；文档自验，未运行全量测试或派独立复核 | Codex |
| 2026-09-06 | 执行前整理 | 核实前置全计划独立技术完成及项目简单改动自验约定；整理阶段内执行顺序、D1—D3 设计交付、已有成果去重和代表性对照任务；地图区分当前主线与宿主支线，更新 B01/B02 | 当前计划执行交接、执行顺序、当前阶段；前置最终报告；只读任务记录 | 文档准备完成，阶段 0 设计中；未开始实现或声称用户接受 | Codex |
| 2026-09-06 | 前置成果只读对照 | F01/F03/R01/R02/R03/E01—E04 部分覆盖，F02/R04/E05 未覆盖；三类分流、场景表、按需契约、字节保护及范围绑定直接复用 | 当前规则、模板和前置阶段 3—4 契约；本轮子代理对照 | 有界差距分析，非独立准入或实现复核 | /root/skill_instruction_audit |
| 2026-09-06 | 执行前文档自验 | 46 个本地链接/锚点、固定标题、反向引用和空白检查通过；已安装 CLI 普通检查退出 0（3 条背景引用告警），仓库 CLI strict-readiness 退出 0（8 条告警，保留宿主及本计划设计阻塞） | 验证方式中的命令；本轮命令标准输出 | 文档检查通过；未运行全量测试、构建、安装或发布，不构成准入 | Codex |
| 2026-09-06 | 阶段 1 实施自验 | 风险分流、共享三参考、guide、模板及受管入口已实现；完整 verify：Python 518 passed/93.42%，Node 101/101；行为演练与资源/字节保护通过 | [实际结果与受测指纹](../fixtures/plan-governance-workflow-streamlining-cases.md#阶段-1-集成结果) | 实施中，待独立完成复核与用户验收 | Codex / runtime 实施子代理 |
| 2026-09-06 | 阶段 1 独立技术验收 | B03/B04 修复独立确认，合并首轮其余通过范围，技术完成通过 | [独立完成报告](../reviews/plan-governance-workflow-streamlining-completion-review-20260906.md#第三轮通过) | 实施中，等待用户验收；未发布/全局同步 | /root/streamlining_design_gate |
| 2026-09-06 | 修复后最终集成验证 | B03/B04 已按独立结论解除；完整 verify 退出 0，Python 585 passed/93.55%，Node 101/101、0 skipped；源码保持最终独立受审身份 | [最终集成验证](../fixtures/plan-governance-workflow-streamlining-cases.md#最终集成验证) | 技术完成，等待用户验收；未提交、发布或全局同步 | Codex |
| 2026-09-06 | 1.0.0 发布与本地更新授权 | 用户要求先更新版本 1.0.0、发布 npm 并更新本地依赖；已确认版本不存在并完成发布预演 | [发布维护](plan-governance-distribution-setup.md#2026-09-06-100-发布维护) | 发布获授权；本计划仍等待实际使用验收，不补写用户接受 | Codex |
| 2026-09-06 | 1.0.0 发布与本地更新完成 | 官方版本/latest 和包校验值确认；全局 CLI 及 Codex/Claude skill 同步完成，安装包与资源逐字节核对及安装后验证通过 | [发布结果](plan-governance-distribution-setup.md#2026-09-06-100-发布维护) | 发布与安装完成；实际减负体验仍待用户验收，未提交 Git | Codex |
| 2026-09-06 | 最近证据限量输出自验 | 显式 JSON 窗口及原文定位已实现；默认输出与门禁兼容，完整 verify 通过 | [实现与验证结果](#workset-最近证据限量输出) | 增量技术完成，未发布；整体实际使用验收保留 | Codex |
| 2026-09-06 | 1.0.1 发布 | 用户授权后通过统一脚本发布；官方版本/latest 与包完整性已确认 | [发布维护](plan-governance-distribution-setup.md#2026-09-06-101-发布维护) | 1.0.1 已发布，本机安装保持 1.0.0；整体实际使用验收保留 | Codex |
| 2026-09-06 | 1.0.1 本地更新 | 用户授权后升级全局 CLI 并同步两套 skill；安装包、资源、限量命令及非受管文件保持验证通过 | [本地更新结果](plan-governance-distribution-setup.md#101-本地更新) | 本地更新完成；整体实际使用验收保留 | Codex |
| 2026-09-07 | 使用反馈规则增量 | 用户确认 U1—U5 实施；三份参考修改、三个行为走读及有界源规则独立完成复核通过，保留旧门禁 | [增量结果](#实际使用反馈与有界调整2026-09-07)；[独立报告](../reviews/plan-governance-usage-adjustments-review-20260907.md) | 源规则通过，安装资源及项目入口待空闲交接同步；整体体验验收保留 | Codex / /root/usage_adjustment_review |
| 2026-09-07 | 本机与项目入口同步 | 用户明确立即同步；两套 skill 资源一致，摩托车受管入口及 ScreenshotStitcher 最小入口已更新，非受管内容与旧计划保持 | [同步完成证据](../fixtures/plan-governance-workflow-streamlining-cases.md#本机及项目入口同步完成) | 同步完成，整体实际使用验收保留；未发布或提交 | Codex / /root/skill_instruction_audit |
| 2026-09-07 | 1.0.2 发布完成 | 用户授权发布下一补丁；统一发布验证通过，官方 version/latest 与包校验值一致，原 registry 已恢复 | [发布维护](plan-governance-distribution-setup.md#2026-09-07-102-发布维护) | 已发布，整体使用验收保留；全局 CLI 仍为 1.0.1，未提交/推送 Git | Codex |
| 2026-09-07 | 1.0.2 本地更新 | 用户授权后升级全局 CLI；18 项包文件与发布内容一致，四个 guide 及 skill 一致性检查通过，既有 skill 文件和 registry 保持 | [本地更新](plan-governance-distribution-setup.md#102-本地更新) | 本地 CLI/skill 一致，整体实际使用验收保留 | Codex |
| 2026-09-08 | 使用反馈与执行抽查 | 用户确认提问减少、先验证可行性可接受；已完成设计样本自动复核和定向修复符合，仍发现重复全文读取 | [本次抽查](#使用验收与执行抽查2026-09-08) | 实施中；剩余读取优化由 AI 定向复查，非笼统等待用户体验 | Codex |
| 2026-09-08 | 调试复核粒度反馈 | 用户指出身份探针任务持续复核；快照中五组变化各有准入和完成复核，包含两组接受条件未变的诊断增量，形成按风险边界合并的提案 | [案例与方案](#调试复核粒度优化提案2026-09-08) | 方案落档；未修改共享规则、旧计划门禁或正在运行的身份任务，非实施验收 | Codex |
| 2026-09-08 | 调试复核粒度源规则实施 | 用户授权后有界设计准入通过，两份参考修改、机械验证及六组独立行为判断通过 | [受审指纹与结论](#本次实施进展) | 源规则通过；未安装/发布或迁移其他项目，真实减负效果仍待观察 | Codex；granularity_gate（独立） |
| 2026-09-08 | 1.0.3 发布与本地更新 | 用户授权后统一发布及全量验证通过；官方 version/latest、校验值、全局 CLI、两套 skill 及非受管文件保持均已核对 | [分发维护](plan-governance-distribution-setup.md#2026-09-08-103-发布与本地更新) | 发布/本地更新完成；项目策略未自动迁移，实际使用验收保留 | Codex |
| 2026-09-08 | 两项目策略接入 | 用户授权后，摩托两项迁移、一项既有策略补齐、八项保留原门待接入；Screenshot 既有策略确认；独立完成核对通过，原状态/全部阻塞/动作类型保持 | [接入结果](#本次实施进展) | 可迁移部分完成，其他计划条件未满足且授权已记录；非业务验收 | Codex；project_strategy_migration_review（独立） |

以上记录按当次工作树和工具版本解释，失败历史保留。前置 D02—D04 现已由原计划独立确认修复，本计划不归功或重审。已有共享目标和背景引用告警须说明归属，不为消除提示添加虚假硬依赖。已安装 CLI 检查不等于仓库新实现验收；文档检查不等于行为通过或阶段准入。


2026-09-06 本次阶段 0 验证：执行样本内存命令得到 legacy accepted 0、risk rejected 3；旧 `guide --help` 只返回检查器帮助，未提供规则读取。严格治理和 `git diff --check` 通过；宿主未完成及当前 B02 警告保持。代码未修改，未运行测试套件或打包。设计输入只读核查者 `/root/streamlining_contract_audit` 确认共享判定落点及快照检查可选边界；此核查不是独立准入。

## 全计划用户验收

以原报告场景为输入，用最终规则完成小修复、普通功能和高影响变更的代表性演练：用户能看到必要的前期澄清和不确定功能预览，小问题自行闭环，复核能解释新增检查或证据复用的原因，技术结果可复验，最终用户接受有记录。相关性能只在演练对象有运行资源影响时观察；报告或 skill 文本不需要强制资源采样。

入口精简的验收关注实际读取是否按需、任务选择是否正确、已安装 skill 或仅安装 CLI 时必要规则能否找到，以及旧文档/用户自定义内容是否保持。两者均未安装时应明确前提缺失，不要求完整规则随项目离线携带。行数或字节减少只是辅助观察。独立技术复核通过后交用户验收，用户接受才关闭本计划；正式发布或同步另按授权办理。

执行体验同时按 E05 对照：恢复后能继续当前工作，跨任务能取得必要证据，复核范围与等待原因可解释，必要反例仍能检出。若文档已精简但代表性任务仍重复取证、验证或等待，应记录剩余问题并调整执行规则，不能单凭结构拆分宣布减负目标达成。

## 最新阶段复核

| 字段 | 内容 |
|---|---|
| 日期 | 2026-09-09 |
| 阶段 | 阶段 1 |
| 方式 | 独立 |
| 风险 | 高影响 |
| 风险依据 | 精简共享提示涉及授权、暂停与验证判断；五类行为走读和机器格式核对通过，检查器及三策略语义保持 |
| 结论 | 通过：GPT-6 提示精简独立检查通过，技术完成，等待用户验收 |
| 证据 | [GPT-6 提示精简实施](#gpt-6-提示精简实施) |
| 复核者 | /root/gpt6_autonomy_check |

## 阶段复核记录

| 日期 | 类型 | 阶段 | 方式 | 风险 | 结论 | 证据 | 复核者 |
|---|---|---|---|---|---|---|---|
| 2026-09-09 | 单次独立完成检查 | 阶段 1 | 独立 | 高影响 | 未通过：业务超时误判检查状态、旧失败缺字段可被迁移掩盖 | [单次复核结论与修复](#单次复核结论与修复) | /root/single_review_check |
| 2026-09-09 | 修复自验 | 阶段 1 | 自验 | 高影响 | 通过：两项独立发现修复自验通过，技术完成，等待用户验收 | [单次复核结论与修复](#单次复核结论与修复) | Codex（实施者自验） |
| 2026-09-09 | GPT-6 提示精简独立检查 | 阶段 1 | 独立 | 高影响 | 通过：GPT-6 提示精简独立检查通过，技术完成，等待用户验收 | [GPT-6 提示精简实施](#gpt-6-提示精简实施) | /root/gpt6_autonomy_check |

## 最新独立准入复核

| 字段 | 内容 |
|---|---|
| 日期 | 2026-09-06 |
| 阶段 | 阶段 1 |
| 结论 | 通过：阶段 1 技术完成验收通过，等待用户验收 |
| 证据 | [第三轮报告](../reviews/plan-governance-workflow-streamlining-completion-review-20260906.md#第三轮通过) |
| 复核者 | /root/streamlining_design_gate |

## 独立复核记录

| 日期 | 类型 | 阶段 | 结论 | 证据 | 复核者 |
|---|---|---|---|---|---|
| 2026-09-06 | 阶段准入及设计完成复核 | 阶段 0 | 通过 | [独立报告](../reviews/plan-governance-workflow-streamlining-design-review-20260906.md) | /root/streamlining_design_gate |
| 2026-09-06 | 阶段准入复核 | 阶段 1 | 通过 | [独立报告](../reviews/plan-governance-workflow-streamlining-design-review-20260906.md) | /root/streamlining_design_gate |
| 2026-09-06 | 阶段完成复核 | 阶段 1 | 未通过：B03 通过句号兼容缺口 | [第一轮报告](../reviews/plan-governance-workflow-streamlining-completion-review-20260906.md#第一轮未通过) | /root/streamlining_design_gate |
| 2026-09-06 | 阶段完成增量复核 | 阶段 1 | 未通过：B03 已解除，B04 问号候选仍误放行 | [第二轮报告](../reviews/plan-governance-workflow-streamlining-completion-review-20260906.md#第二轮b03-通过b04-未通过) | /root/streamlining_design_gate |
| 2026-09-06 | 阶段完成复核 | 阶段 1 | 通过 | [第三轮报告](../reviews/plan-governance-workflow-streamlining-completion-review-20260906.md#第三轮通过) | /root/streamlining_design_gate |

## 实际使用反馈与有界调整（2026-09-07）

用户询问两个项目的真实任务是否符合减负预期，并进一步问“怎么调整”，随后以“可以”确认执行方案。本节作为阶段 1 内 R01—R04、E01—E05 的有界增量，不新增计划、阶段或 skill；授权落实共享参考、适用项目入口及本机 skill 资源同步，不含 npm 发布、CLI 包安装或旧阶段策略迁移。原技术验收和 1.0.1 结果保留，不能扩充为本次增量已通过或整体体验已验收。

**观察基线**：截至 2026-09-07 21:28（北京时间）的可见对话、工具活动及项目文档，不统计 reasoning、加密消息或推算 token。任务仍运行的部分只按快照判断。

| 任务与定位 | 已有效的行为 | 本次拟改进之处 |
|---|---|---|
| 查看 ScreenshotStitcher 交接文档；`01a076a8-b5b0-7ae3-a701-1c8141389abd`；20:19—21:28 | 先交互预览、按用户反馈删补选；用户要求逐项确认；自动接缝误设人工确认的问题被发现后局部重审 | 系统选图、iPhone 等已确认决定的同步仍再次派发检查；四次新代理继承完整对话。尚无完整治理接入或最终运行/性能验收证据，不把所有协作者算独立复核 |
| 分类未完成计划；`01a07b97-f094-7c02-8471-b6677406bbcc`；19:19—21:28 | 使用限量 workset；用户主动新增托管模型需求，身份/支付/退款/账本审查有实际价值 | 限量后仍回传大量计划正文和阻塞；连续产品探索可按一组已收敛决定集中核对，不能只按文档改动次数安排复核 |
| 推进网站收尾计划；`01a07ba1-95cf-7de3-b52e-14b74f3377b6`；19:29—21:22 | 公网暴露被查出，授权后的重载失败自主回滚与修复，实际变更后独立复验 | 19:47 已明确待复核却等用户20:00再安排；未来续期混入网站关闭条件；21:01/21:04/21:08 三轮范围复核中，待生成的新结论与旧记录形成循环 |

观察时的接入限制：摩托车项目的 `docs/data-quality/plan-document-format-update-20260906.md` 明确只更新活跃计划格式、保留旧独立门禁；根 AGENTS 仍为旧入口，托管 AI 计划也明确沿用独立准入。不能把这些任务当作完整启用新风险策略的对照实验，更不能据此自动迁移旧计划或抹去有效失败。ScreenshotStitcher 的普通工程说明和产品文档也不足以证明已接入治理；是否需要治理按实际范围判断。

### 本次增量准入与范围

- 2026-09-07 独立只读复核者 `/root/usage_adjustment_review` 确认：“通过：U1—U5 的有界设计增量可实施，未发现设计阻塞。”仅覆盖本次规则增量准入，完成和用户验收另行判断。
- 实施身份：HEAD `f8c6e3728df4b4dae33b951fe003e487062f7dd9` 加已有文档修改；开始时三份源参考与 Codex/Claude 已安装资源均一致。planning / verification / cli 的 SHA-256 分别为 `d4adf7979ef3b6ec1a60d78baff3f5cd6008d2913fd3ac4f4ad5253a9d1824b2`、`2717a9daf80d59124397b7d4d0e1faed1450be486554cb9f290c2b2b528080f6`、`28a7e726ad8fcebe4b85ae3918a50d1a89fbce2eb4bae498b086fd976c7e4ce6`。
- 实现只修改 `resources/skill/references/planning.md`、`verification.md`、`cli.md`；SKILL 入口、checker/JSON/模板/版本号/代码测试保持。文档证据记录于本计划、现有执行样本和本次独立报告，地图同步实际进度。
- 源参考通过后，使用仓库 Node CLI 的现有 setup 同步 Codex/Claude 的 manifest 内资源；对比安装基线并保留未受管内容。此为已授权的本地开发规则同步，npm CLI 的 `guide` 仍来自已发布 1.0.1；一次工作选择同一规则来源，不混读。未发布规则不伪称 npm 包已更新。
- 摩托车项目已有治理，入口更新限 AGENTS/CLAUDE 受管块，旧阶段门禁保持；ScreenshotStitcher 只补与现有产品文档相容的最小发现入口，不为有限原型创建全套治理目录。原拟空闲交接时操作；用户后续明确“没事，同步吧”，本次按新授权立即同步。
- 不改变其他任务的业务实现、计划受审内容、有效失败或业务结论；进入完成复核后冻结本次参考，后续仅落档和逐字同步。完成复核只检查本次差异、场景及适用证据，不重跑原全量验收。

### 已批准的调整

下列规则优先以少量替换和补充落入既有参考，不扩写入口、复制整套流程或新增必填报告。共享规则变化的适用验证按影响范围确定；高影响复核边界不能由实施者用本次提案自行豁免。

| 调整 | 具体执行规则 | 拟落点与验收反例 |
|---|---|---|
| U1 接入与策略分清 | 接手时区分“仅更新文档格式”“入口已更新”“当前阶段已显式选择风险策略”。治理任务先按实际发现的入口读取同源规则；确需升级项目入口时只改受管块，旧计划不自动迁移。符合条件的新计划用风险分流；旧阶段变更策略须按现有兼容规则处理 | planning / cli；不能因安装了新版 skill 就宣称旧独立门禁消失；不为无治理需要的原型强建完整目录 |
| U2 按一组已收敛决定安排复核 | 当前 AI 自行查证、汇总已确认决定并做局部文档自查；独立复核集中在必要的高影响方案收敛、适用阶段门或有效失败修复后。身份、权益、支付等实质变化及时定向检查；不要积压需要先判断的关键风险。已有授权且门禁需要复核时自动安排，不等待用户再说“复核吧” | verification；“确认仅支持 iPhone→同步相应段落”默认不派独立者；“退款会影响已发权益”不能因只改文档而省略必要判断 |
| U3 先界定审查对象，再生成结论、落档 | 送审用简短任务说明区分当前交付、后续事项及通过后记录同步。独立者基于当前范围、实际证据和原失败原因判断，不要求“本次通过记录已存在”作为出具本次结论的前提。通过后执行者追加结论并同步状态，纯落档做适用文档检查；有效独立失败仍由适格独立者解除 | verification；原失败记录未被覆盖，新独立判断可追加；若范围/证据又变或发现有效缺陷，补相应复验，不能预先自填通过或用正文排除有效阻塞 |
| U4 分开当前交付与未来运维 | 发现验收涉及未来周期或新增范围时尽早说明取舍；经用户确认后，当前交付按已确认的新范围验收，未来事项在适合的现有或独立计划中承接。新计划尚未准入本身不阻塞当前交付；若欠缺会影响当前运行安全、恢复能力或已承诺的验收，则仍是当前阻塞 | planning / verification；网站可在续期职责和后续要求妥善承接后关闭，不必等待下一次真实续期；不能以拆计划规避当前证书、暴露或恢复缺陷 |
| U5 读取与交接按缺口展开 | 先看当前工作集/阶段/差异与少量最近证据；大型结果先在工具内选取当前任务字段，不先截断后再倾倒全文。只有相关缺口、变化或冲突才展开。复核交接提供范围、差异、风险、可访问证据、完成条件和授权边界，默认不继承完整聊天；必要历史按需查。反馈回传新增发现和依据，不重复全量日志 | planning / verification / cli；保留完整数据派生的所有有效阻塞，不把显示裁剪当作检查范围裁剪；完整记录仍可回查，不新增隐式清除历史的 CLI 行为 |

### 本次实施结果

三份共享参考已落实 U1—U5；SKILL 入口、代码、checker/JSON、模板、版本号保持。2026-09-07 [独立复核](../reviews/plan-governance-usage-adjustments-review-20260907.md)结论为“通过，本次三份共享参考的 U1—U5 源规则增量完成复核通过，无源规则阻塞”。三个行为输入组的判断与机械结果分别见[增量验证](../fixtures/plan-governance-workflow-streamlining-cases.md#2026-09-07-使用反馈增量验证)。

本机资源与两个项目入口已同步。用户在获知任务仍活跃后明确“没事，同步吧”，本次按该授权立即执行；原等待安排不再阻塞这次同步，通用同步规则没有改写。

Codex/Claude 各 10 项 manifest 资源与受审仓库一致，未受管内容保持；摩托车 AGENTS/CLAUDE 只更新受管块，52 份计划相关文件无变化；ScreenshotStitcher 仅新增最小 AGENTS 入口，不创建全套治理目录。实际命令、检查、指纹及旧文件回滚副本见[同步完成证据](../fixtures/plan-governance-workflow-streamlining-cases.md#本机及项目入口同步完成)。

已[发布 1.0.2](plan-governance-distribution-setup.md#2026-09-07-102-发布维护)，官方版本、latest 与包校验值已确认。本地 npm CLI 随后也已[更新至 1.0.2](plan-governance-distribution-setup.md#102-本地更新)，guide 与已同步 skill 的规则一致。本轮没有新增源规则缺陷，不因逐字同步再次独立复核；实际减负效果和整体用户验收继续保留，不能由同步完成代替。

### 推进顺序与完成判断

1. 先把 U2—U5 的重复/缺口收敛到共享参考；U1 说明接入前提。本轮已获实施授权，按本次实际差异和证据验证，不将旧版本的通过结论挪作本次证明。暂不需要改 checker、JSON、生命周期枚举或另造调度器。
2. 按授权更新适用项目入口并明确策略；本次用户明确立即同步，已完成。项目接入与历史阶段迁移分别处理，不批量更新历史文档或覆盖其他任务内容。
3. 用本节三个实际反例做有界行为走读：小决定的文档同步、带旧失败历史的范围拆分复核、有限证据后的按需读取。高影响场景在必要设计复核中一并检查，不为每个样本单设门禁。
4. 再观察下一次低风险改动和一次必要高影响复核：小问题自行闭环、必要复核自动衔接、旧失败保持、未来事项归属明确、无结论与落档循环、证据可访问。实际产品功能/性能及用户接受仍按原约定验证；不要求固定提速比例，也不以工具次数或文件大小替代 token/耗时结论。

先前方案检查只说明提案文档自洽。原版本已交付，本次规则增量的验证另行记录，实际使用效果继续观察；不将正在运行的原型或未量化的收益判为通过，也不因可选补证重做原全量测试。

2026-09-07 本次文档自验：新增锚点、地图六列结构、固定标题、反向引用及空白检查通过；`PYTHONDONTWRITEBYTECODE=1 plan-governance-cli check .` 退出 0，0 ERROR、14 WARNING，保留现有宿主、共享范围和背景引用提示。只形成使用反馈提案，不是阶段准入或新规则行为通过；未运行代码测试、构建、发布或安装同步。

## 使用验收与执行抽查（2026-09-08）

用户反馈：“问我的确实少了，第4点可行性先验证也没问题，其他的我感知不强，其他的可能得你自己看看”。据此记录确认次数减少、先验证可行性的做法可接受；其余执行行为由 AI 查证，不继续笼统要求用户感知后台动作。这不是所有项目的功能、性能已通过，也不是用户明确接受整个计划关闭。

本轮只读抽查三个任务：两个已完成设计轮次，以及正在运行的恢复/支付准入轮次。任务仍活跃的部分按读取快照判断，不把尚未交付认定为失败；没有同等任务计时或 token 数据，不报告提速比例。以下只使用可见用户/助手消息、工具活动和项目证据，不将协作者数量直接等同于独立复核次数。

| 核查项 | 实际证据 | 结论 |
|---|---|---|
| 必要复核自动衔接 | “查看 ScreenshotStitcher 交接文档”在 2026-09-08 09:14 的“继续细化第二批”轮次内自动启动 second_batch_design_review；“分类未完成计划”在 09:18 的问答网关设计轮次自动衔接既有 identity_probe_review，均无用户中途追加“复核吧” | 样本符合；未把允许细化设计当作自动授权代码实现 |
| 修复与重审有实际对象 | ScreenshotStitcher 裁剪确认/撤销语义有歧义，修正后原复核者定向通过；摩托车修正跨设备权限顺序、非法请求与正常工具错误、终态缓存清理后，原复核者确认原问题解除，再落档 | 样本符合；保留必要质量检查，没有看到先要求本次通过记录已存在的循环 |
| 验证范围与变更相称 | 上述两个纯设计轮次可见的是链接、JSON 示例、diff 与治理检查，没有看到因纯文档落档重跑代码全套测试；失败后存在实际修正，不能将所有重复 check 都算无效 | 本次样本未发现无理由全量代码复验；不能推广为所有任务都没有重复 |
| 按需读取与输出 | 支付准入轮次相邻两次全文读取 planning/verification，未见中间参考修改；ScreenshotStitcher 设计轮次重复全文读取 page-wireframes，并读完三个参考。当前审计自身也发生过未先筛选任务结果而回传过长内容，随后改为按条目筛选 | 尚未充分落实；部分首次读取、修改后回查或恢复读取可以合理，不能把每次重复都当成缺陷，也不能据此声称上下文已显著下降 |

证据定位：ScreenshotStitcher 任务 `01a076a8-b5b0-7ae3-a701-1c8141389abd`，已完成轮次 `01a07e94-7d97-7092-b00f-803aea63695c`，以及其 `docs/plans/first-release-roadmap.md` 的“阶段复核记录”与“最近实施/验证记录”；摩托车“分类未完成计划”任务 `01a07b97-f094-7c02-8471-b6677406bbcc`，已完成轮次 `01a07e98-1dc3-7280-b3c5-54ad3862fc27`，及其 `docs/plans/managed-cloud-ai-service.md` 的问题协议修正记录。支付观察来自“评估真实支付测试准入”任务 `01a080bd-bad7-7a70-a6cd-b063138e3273`，轮次 `01a080be-0588-77f3-a407-d3356e92a305`；重复读取调用为 `exec-4f83d3aa-937d-4590-ae0f-688e65590f61` 与 `exec-4319acab-f75a-49c8-a4e7-d4f39bc6facb`。上述事实要点已记录于本节，不以工具 ID 代替证据内容。

本次抽查最初识别的剩余项为 E01/E02 的执行落实：按当前缺口读取相关段落；同轮已取得且未变化的规则复用；大型任务结果在工具内先筛选，再回传当前证据。随后用户提供身份探针任务，新增[调试复核粒度问题](#调试复核粒度优化提案2026-09-08)，不能再将剩余项限定为读取。后续定向复查由 AI 承担，不要求用户判断 token 或后台读取；当前保留实施中，不将部分改善记成全部达标，不重开原技术验收。

## 单次独立复核提案（2026-09-09）

用户提出进一步简化：仅高风险或高影响改动需要独立复核，同一范围只复核一次；复核指出的问题由实施者修复并自行判断验证结果。本节最初记录需求与建议，随后已获实施授权并完成[规则、检查器及修复自验](#单次复核结论与修复)；下方 2026-09-08 记录保留为历史事实。

### 建议规则

1. 普通改动由实施者自验；只有实际高风险或高影响改动才安排独立复核，按权限、安全、隐私、资金、数据及影响范围等实际后果判断，不按目录或流程节点触发。
2. 同一交付范围只进行一次独立复核，通常放在实现和自验完成后、交付或执行高影响外部动作前；必须先判断的高影响设计可将这一次前置，不再追加完成复核。
3. 复核发现的问题由实施者修复，逐项验证原问题与受影响回归，记录“独立复核发现 → 修复 → 自验结果”，不再送独立重审。这同样适用于原高风险范围内的修复，不以修复仍涉及高风险为由重新触发。
4. 验证失败或问题未解决时继续修复或如实报告阻塞，不能因已经审过一次就记为完成。超时、不可用或没有实际结论不算完成了一次复核。
5. “一次”以已明确的交付范围为单位，不能把每个问题或修复拆成新范围；确实新增需求或新的高风险影响时，只对新增范围重新判断。独立复核不替代外部操作授权。

该方案把独立复核的职责收敛为一次查漏，修复正确性最终由实施者及验证证据负责。它减少往返，但不再保证修复结果经过独立确认；保留原发现与真实修复证据，不把自验写成独立通过。

### 精简与落地范围

只读基线：当前已安装的 verification 共 91 行、约 4,600 字符，包含复核、机器记录、阶段门和用户验收，不全是复核限制。复核约束分散于风险分流、旧计划兼容、失败恢复和调度；建议以以上短规则替换重叠的复核执行要求，机器字段与兼容说明单独保留，避免继续叠加例外。

这次涉及行为改变，不能只改文案：`scripts/check_plan_governance.py` 当前只允许完整独立通过解除原独立失败，`tests/test_risk_review.py` 和 `tests/test_workset_evidence_limit.py` 有对应回归。实施时需统一 skill 入口/共享参考、记录与检查器语义、相关测试；修复自验须能关闭已验证的问题且保留原独立发现，高风险任务也不能被机械规则强制无限重审。旧计划如何显式采用此策略及既有失败如何按新策略验证，需要在实施中一致表达，不能抹除历史或因迁移直接标记通过。

后续验证聚焦：普通改动零次独立复核；同范围高风险改动一次；多项发现修复后自验闭环；未解决问题保持未完成；新增高风险范围重新判断；超时未完成检查不冒充已复核；旧记录兼容且自验不伪称独立结论。本轮仅记录需求及建议，不派独立复核，不运行代码测试、构建、安装或发布。

本轮文档验证：`git diff --check` 与 `PYTHONDONTWRITEBYTECODE=1 plan-governance-cli check .` 通过，检查器报告 13 条既有 WARNING；新增章节、地图链接和计划引用检索完成。只验证提案落档，不代表新策略已实现或通过行为验收。

### 单次复核实施（2026-09-09）

用户随后明确“按照这个调整”，授权规则、检查器及相关测试实施，并以本节已确认的单次复核方式执行本次变更。当前工作树基于 `179982c`，保留交接以来的未提交差异。本次只在仓库实施，不重复发布、安装或跨项目迁移。

采用显式 `复核策略：单次独立复核`，新模板默认使用；旧 `风险分流` 和缺省独立格式仍可读取，选择新策略后按新语义处理，不要求为迁移先独立重审。复用八列记录，以类型 `修复自验` 标记独立发现后的闭环，证据说明已审范围、各问题修复及回归；高风险/高影响范围内的后续自验可复用一次有效独立检查。范围真实性由执行者依据差异与证据判断，检查器不假称自动识别风险或范围漂移。

Step 0 为检查器现有“独立失败只能独立恢复”的分支及旧策略回归；新增正反场景覆盖一次检查后的修复自验、缺少检查/证据、检查不可用、未修复、旧策略兼容及 workset/hook 一致性。治理阶段将一次检查安排在设计准入，实施与修复后自验；不再加完成独立复核。本次规则与实现收敛后只安排一次独立完成检查，其发现由实施者修复并自验。适用验证采用定向回归及 `npm run verify`；失败保持未完成并修复，用户体验验收仍保留。

实施前旧策略基线：`python3 -m pytest tests/test_risk_review.py tests/test_workset_evidence_limit.py -q --no-cov` 为 265 passed。新增单次策略后定向检查为 298 passed；再补两项限量 workset 样本后，`npm run verify` 退出 0：648 个 Python 测试通过，覆盖率 93.62%（门槛 85%）；103 个 Node 测试通过、0 skipped；严格治理保持 13 条既有 WARNING。`quick_validate.py resources/skill` 与 `git diff --check` 通过。

真实 Node → Python CLI 在临时项目以 `workset --json --strict-readiness --evidence-limit 1` 验证：普通改动及独立发现后修复自验均为 `ready/implement`、退出 0；修复仍失败为 `blocked/resolve_blocker`、退出 1；高影响自验缺少独立基线为 `unknown/unknown`、退出 1。未改变 JSON schema 1 或动作枚举，检查仍只验证结构，不证明证据内容。当前源 verification 从已安装基线约 4,600 字符缩至约 3,800 字符，复核执行收敛为四段；不据此推算耗时收益。

本次唯一独立检查已安排 `/root/single_review_check`，检查期间规则、代码和测试范围冻结；结论及适用修复自验在此追加，不再安排第二轮。

### 单次复核结论与修复

`/root/single_review_check` 完成唯一一次独立只读检查，结论为未通过，提出两项 P2：一是对失败结论全文搜索“超时”等词，使已完成检查发现的业务超时问题无法修复闭环；二是迁移恢复只核对旧记录日期，可能漏掉较早失败记录的证据或身份缺失。复核者实际执行 300 项定向测试并用内存样本复现两项问题，未修改文件。

实施者修复并自验：独立基线按明确结论前缀区分检查结果和未完成状态，不再搜索业务描述中的关键词；复核未完成直接写超时/不可用状态，不能伪填业务缺陷结论。单次策略迁移时逐条核对旧记录的日期、类型、证据及身份，缺口保留。`tests/test_single_review.py` 新增业务超时/不可用/证据失效三个正向场景，以及旧失败缺证据/身份/类型三个反向场景；两项发现均已由实施者确认修复，没有再次独立送审，也不把自验写成独立通过。

修复后 `python3 -m pytest tests/test_single_review.py tests/test_risk_review.py tests/test_workset_evidence_limit.py --no-cov -q` 为 306 passed；最终 `npm run verify` 退出 0，654 个 Python 测试通过，覆盖率 93.63%，103 个 Node 测试通过、0 skipped。日志分别为 `/tmp/plan-governance-single-review-final-verify.log`；长期证据以本节结果和仓库可复验命令为准。

独立行为走读也符合单次规则：高影响权限变更一次查出越权、敏感日志和回归缺口后，由实施者逐项修复及验证，任一未解决则保持未完成，全部解决后同步结果，不按三项修复再拆三轮审查。新高风险需求只判断新增范围。

当前计划已显式采用单次策略，原六列历史及旧独立结论保留；本次八列记录准确区分原发现与修复自验。技术完成但整体使用验收仍保持实施中、等待用户验收。仓库源规则与已安装 1.0.3 已有差异，本次检查使用 `node bin/plan-governance-cli.mjs`，不混用安装版规则或检查器；尚未安装、发布、提交或迁移其他项目。

策略同步时发现旧最新摘要与历史末条引用同一报告但链接标签不同，已统一标签，保留原结论、日期、复核者和历史。文档同步后仓库 CLI 严格检查及 `git diff --check` 通过，仍为 13 条既有 WARNING；当前 `workset` 保持实施中、等待用户验收，不以本次技术通过关闭整体计划。

## GPT-6 官方实践提示审查（2026-09-09）

用户在提交 `c9da6e1` 后要求按 OpenAI GPT-6 官方最佳实践审查 skill，目标是减少规则冲突、提高自主性和交付效果。已实际打开 GPT-6 Astra 模型指南与官方 Build skills 文档，并对照当前入口、三份参考、模板及项目入口，形成[六项发现、提示候选和后续验证场景](../reviews/plan-governance-gpt6-skill-audit-20260909.md)。优先处理强制访谈收口、宽泛暂停条件及策略/来源分流，再合并重复内容与覆盖率模板矛盾。

本次是用户请求的提示审查及建议落档，未修改现行 skill、检查器或安装版，不增加独立复核轮次，不将建议伪称已实现或已证明提速。先前单次复核实现与修复自验事实保留；这些建议不是对上一轮技术门禁的重新裁决。整体计划仍实施中，使用效果未接受，后续行为调整按新的明确实施授权推进。

### GPT-6 提示精简实施

用户随后以“可以”确认六项建议，并询问是否还有必需确认；当前范围与取舍均已明确，直接实施。基线为 `c9da6e1` 的 skill/入口及本轮三份待提交审查文档。修改入口、三份参考、计划模板、生成器中的项目入口文案及本仓库 AGENTS/CLAUDE；检查器、JSON 格式、策略语义与版本保持。本轮不安装、发布或跨项目同步。

Step 0 为上述审查定位的强制访谈、宽泛暂停、策略/来源混用、计分触发、重复提示与模板覆盖率矛盾；完成条件是六项有对应精简、固定格式与真实阻塞保持、已有授权内能继续交付且不能越权。复用五类行为场景，增加“用户仅授权文档”和“开发源码与安装版不同”的边界；按当前单次策略在实现收敛后做一次独立行为检查，重点判断自主性表述是否改变授权边界及旧策略兼容，不重开原单次复核实现的审查。必要发现由实施者修复自验。适用验证为 skill/链接及资源一致性检查、项目要求的 `npm run verify`；不以字符减少替代行为证据。


实际完成：删除机械计分，入口明确用户指令相对流程建议的优先级；grill-me 改为可选，取消重复访谈收口；暂停条件聚焦实质边界变化；先选当前有效策略与来源；planning 合并事实源/恢复说明，格式细节放附录；纯文档覆盖率可记不适用。README 与 agents 元数据同步，生成器/本仓库受管入口保持一致。检查器与 `c9da6e1` 逐字节一致，未修改三策略或 JSON 契约。

本轮唯一独立只读检查 `/root/gpt6_autonomy_check` 明确通过，未发现当前范围的提示冲突或必要机器契约丢失。五类原始输入走读均符合：多轮日志小修复不强制治理；仅文档授权且无 grill-me 时继续可完成方案但不改代码；单次策略修复自验、旧风险分流仍按旧恢复；纯文档不跑无关测试；开发 skill 使用仓库源。实际核对四主题 guide 输出字节、生成器与受管块、模板摘要/八字段/八列，以及纯文档覆盖率不适用检查。没有第二次独立复核。

验证中同步了三条过时文本断言（强制探索确认标题和两条模板 attestation 字段列举），保留固定结构、文件字节一致性及实际行为测试；setup 的关键词断言改为源文件字节一致性。最终 `npm run verify` 退出 0：654 项 Python 测试通过、覆盖率 93.63%；103 项 Node 测试通过、0 skipped；严格治理保留 13 条既有 WARNING。最终日志 `/tmp/plan-governance-gpt6-autonomy-verified.log`。skill quick_validate、Markdown 链接/锚点及差异检查通过；落档后仅补适用文档检查。

文本比较：入口 1,692→946 字符，planning 4,693→1,792，verification 3,818→2,727；日常入口与两参考合计 10,203→5,465，减少约 46%。CLI 技术手册仍按需读取。只记录字符变化和上述行为证据，不宣称 token、耗时或项目交付成功率已有实测改善。当前仓库技术完成，整体保持实施中等待用户体验验收；未安装、发布或提交本轮差异。

后续用户明确要求发布 1.1.0 并同步本地，现已完成[官方发布、全局 CLI 与两套 skill 更新](plan-governance-distribution-setup.md#2026-09-09-110-发布与本地更新)；上述未发布/安装表述保留为实施轮结束时的状态，整体实际使用验收继续保留。

## 调试复核粒度优化提案（2026-09-08）

用户指出“查看身份交接文档”持续复核，随后询问如何优化，并澄清该任务是新开的会话。本节承接 R01—R04/E05，先形成提案；随后用户授权的源规则实施结果见[本次实施进展](#本次实施进展)，不将其等同于安装资源或其他项目门禁已改变。

### 实际基线与原因

任务 `01a080e0-afc9-7d42-afda-5a22492ee9d8`、轮次 `01a080e1-7d45-7261-976e-001cb5e022be` 的读取快照显示，同一主复核者 `identity_safety_review` 对五组变化分别作了准入与完成判断，共十次已完成判断：Sandbox app ID 兼容、证书失败诊断、叶 AKI 兼容、flags 诊断、assertion 特定格式兼容。后三种兼容中的两种与首项均改变安全接受路径；两组诊断则声明原接受条件不变。不是十名复核者，也不是同一未修正阻塞送审十次。原始事实见[身份实验记录](/Users/jafish/Documents/work/motorcycle-manual-app/docs/data-quality/managed-ai-identity-validation-20260908.md)的“沙盒契约与错误可观察性修正续接”及其后续记录；本节保留要点，外部项目继续追加不影响本次快照范围。

现行 verification 要求在关键设计或完成边界进行必要独立检查，并未规定安全目录每次修改都固定双审。不过目标计划摘要仍保留旧的“最新独立准入复核”，没有显式选择风险分流；其既有门禁不能直接取消。因此问题是诊断准备不足、风险对象划分过细与旧门禁约束叠加，不能仅归因为执行者违反新策略。自动衔接已经有效，不代表复核次数已经合理；没有同等任务计时，十次判断不能换算为可节省时长。

这里的“旧”指项目计划的兼容格式与门禁约定，不指会话创建时间，也不表示任务用了旧版本 skill。新会话读取同一份计划时，当前 skill 的“旧计划兼容”仍要求保留该门禁。仅更新安装资源或重新开会话不足以完成项目策略迁移；旧门禁也没有自动推出“每个诊断增量都双审”。

### 拟议行为

| 调整 | 可执行边界 |
|---|---|
| 按实际变化选择复核点 | 不以目录、文件保存、发现一个错误或完成一次真机回放作为新门禁。送审简报用一句话说明本次新增风险、当前差异以及已有结论覆盖不到哪里；没有新风险或范围变化时引用既有结论，不新建复核任务。 |
| 在首轮设计中准备受限诊断 | 沿已知验证链一次规划固定失败阶段、允许的有限数值、接收者/落盘位置和清理方式。只补确有诊断价值的字段，不默认保存原始证书、身份材料或异常全文，不提前继续执行失败后的认证步骤来收集更多结果；遇到无法预见的信息缺口仍可增补。 |
| 已覆盖边界内的诊断由实施者闭环 | 在项目适用策略允许、无有效独立失败且验证充分时，只增加既定白名单内的固定分类/数值，且不改变接受条件、控制流、输出对象/渠道、留存范围、响应契约及外部操作边界，可由当前 AI 定向自验。诊断有新的泄漏、执行路径或共享影响时先按该风险安排必要独立检查，不因称为“日志”就自动低风险。 |
| 同范围必要检查合并 | 高影响设计先按现行门禁明确不变量和准许范围；范围内多项实现、诊断与受影响回归收敛后，由原独立复核者检查已审基线以来的累计差异；复核者仍须未参与实现或测试编写。只有落在已审查范围内才复用设计结论；新的兼容/接受路径须先重新评估并满足适用实施前门禁，不能打包到最终复核来追认。受审范围冻结，变更及时通知；依赖完成门的真机复测须等结论。 |
| 复测与落档有明确停止条件 | 源、依赖、相关配置/环境和验收契约未发生未覆盖变化时，复测同一批准的构建或仅同步证据无需再审；新会话的资源/签名匹配等必要检查仍做。独立失败修复后只送原阻塞、差异及受影响回归，旧有效失败、超时和证据冲突不因合并而清除。 |

本案例优先消除的是两组诊断机械拆成前后双审的做法。具体可减少几次取决于诊断安全边界能否被已有效的检查覆盖，不能保证十次压成固定次数；三项兼容依次由真实证据揭示，不能事后假定它们一开始即可合并。目标是每个保留的复核点都有独立理由，不设固定复核次数上限，也不新增强制表格或逐项签收。

### 落地范围与验证方案

拟首先精修 `resources/skill/references/verification.md` 的风险分流、复核粒度及增量复验段落，优先替换重叠表述，不再扩张入口或另建调度器；仅有直接交叉引用需要时同步 planning。暂不需要改 checker、JSON 字段或阶段枚举。共享规则本身影响门禁，后续实施继续沿用本计划既有准入及适用独立检查，不能由本提案自验批准自身。安装资源、发布及目标项目策略迁移分别按实际授权处理。

项目接入同时给出一次性的明确路径：只识别正在处理的计划/阶段是否已采用风险分流；对未采用者核对原独立约定、有效失败和当前证据，符合现行迁移条件且有授权时显式更新当前阶段结构，保留原历史，并按实际风险继续执行。已有授权充分时不重复向用户索取；条件不足则指出具体门禁，不批量迁移所有历史计划，不以某个小切片通过解除完整阶段阻塞。后续会话复用已落档策略，不每次重新走一遍迁移。

用四个有界场景验证方案，后续可与同范围独立完成检查合并，不为每例再派复核者：

1. 已批准的诊断范围内补一个固定错误阶段，输出对象和拒绝结果不变：适用策略下定向自验，不新开准入/完成双审。
2. 同样名为“诊断”，却新增原始身份字段落盘或扩大返回对象：识别实际隐私/权限风险，不能按上一例放行。
3. 真机发现新证书例外，需要改变接受条件：先补失败证据、范围和安全设计，保留适用独立实施前检查，实施后验证该差异及回归。
4. 原复核曾失败，或项目仍有旧独立门禁：引用已通过的其他检查不能解除它；仅修正文档链接也不制造新的独立检查点。

完成判断是受影响规则及正反场景符合，并能在后续实际样本中解释复核点的保留/合并依据；不以更少工具调用或机械文档通过宣称安全性、时间或 token 改善。本轮仅改计划和地图，保留其他任务工作树与运行状态。

本轮文档自验：新增章节及地图锚点、地图六列结构、外部项目证据路径和 `git diff --check` 通过；`PYTHONDONTWRITEBYTECODE=1 plan-governance-cli check .` 退出 0，0 ERROR、13 条既有 WARNING。只读方案咨询补充了诊断输出边界、累计差异与独立性约束，不记为准入/完成复核。未运行代码测试、构建、安装或发布；四个行为场景仍为后续验证方案。

### 本次实施进展

用户随后明确“优化吧”，授权落实本节共享规则调整。当前基线 HEAD `179982ca67b37b975bacaf3726c31030f6815b64`，三份已有脏文档保持；verification/planning 源仍分别匹配上轮受审指纹 `cdae5b625a5f4fd1697f3eca3e18565a2cd6a0affad50aeb00c687273782bf7e`、`19ccc39f654af72cf21ea0817a2281287cc395d3e42f982501920cb8f418c8af`。本次 Step 0 是上述五组双审实际样本及当前规则缺少诊断粒度/一次接入说明；适用验证为结构、链接、真实 guide 字节一致性及有界正反行为走读，不重跑未变化的代码全套。失败时保留原门禁并修正规则，独立失败由原复核者定向确认解除。仅修改两份共享参考及本仓库证据文档；安装、发布及其他项目策略迁移不在本轮范围。


2026-09-08 `granularity_gate` 独立只读设计准入通过后实施。实际修改限于 `resources/skill/references/verification.md` 的复核粒度、诊断范围、累计差异与旧策略接入，以及 planning 的直接引用。SKILL 入口、模板、manifest、checker、CLI 与版本均未修改；没有在源规则通过前修改目标项目门禁。

机械验证：skill-creator 的 `quick_validate.py resources/skill` 通过；真实 `node bin/plan-governance-cli.mjs guide verification` / `guide planning` 的 stdout 与源逐字节一致；两份参考本地链接/锚点和 `git diff --check` 通过。仓库 `node bin/plan-governance-cli.mjs check . --strict-readiness` 退出 0，0 ERROR、13 条既有 WARNING。代码未变，不重跑原全量代码测试；这些检查不代替行为判断。

冻结受审 SHA-256：verification 为 `09b01f93ac0bb7715e6565afe9d6c64dd6e0509c2a49f2e2ed5a770dce6e8074`，planning 为 `41817dca005cdcf9bf0aee452e7964134d8607e058d3a61b911d6c90e63ca8e2`。同一 `granularity_gate` 未参与实现或测试编写，独立核对 HEAD、指纹、完整源差异、保留规则和空白检查，结论为“独立完成复核：通过，无阻塞”。下列为其实际判断摘要，不是执行者预填通过：

| 输入场景 | 独立判断 |
|---|---|
| A：已采用风险分流、无失败，原设计枚举内补现有拒绝分支诊断，接受/控制流/对象/渠道/留存/契约均不变，定向验证充分 | 当前 AI 自验并记录，不新增准入/完成双审 |
| B：名为诊断，实际新增底层异常全文与设备材料的持久日志 | 先明确隐私、输出/留存/清理边界，按高影响进行必要独立检查及适用授权，原自验结论不覆盖 |
| C：新 flags 组合从原拒绝位置进入后续验签，样本最终仍拒绝 | 已改变控制流，先补失败证据/安全设计并满足实施前门禁，不能凭最终拒绝判不变 |
| D：新版 skill、新会话，但计划无策略字段、有未解除独立失败，用户仅要求继续调试 | 保留门禁与失败，授权内补证修复后由适格独立者解除；不自动迁移 |
| D2：另一旧计划原门禁通过、证据齐全且明确授权风险分流，随后再开新会话 | 一次更新当前阶段结构并保留历史；后续核对策略及当前证据后复用，不重新迁移 |
| E：合并同设计两次增量，期间受影响依赖变化，完成复核未返回，准备安装复测 | 通知复核者并覆盖累计差异、补受影响验证；依赖完成门的安装复测等待结论，失效证据不复用 |

本次是已有设计上下文的规则走读与源差异检查，不声称盲测、真实项目回放或耗时/token 改善。独立通过后只转写本段及同步地图，不因落档再次送审。源规则交付完成；已安装 skill/CLI 仍为上轮资源，其他项目计划没有迁移，也没有给活跃任务发送指令。安装同步、发布和实际效果观察保持各自边界，整体计划继续实施中。

后续用户授权“发布npm，然后更新本地”，已完成 [1.0.3 发布与两套本地资源更新](plan-governance-distribution-setup.md#2026-09-08-103-发布与本地更新)；上述未安装/未发布说明保留为源实施轮结束时的状态，其他项目策略仍未迁移。

用户随后授权 motorcycle 与 ScreenshotStitcher 项目迁移：摩托支付/上传订单两项显式迁移，证书一项补齐已选择策略的结构，其他八项因原阶段门禁/独立失败保持待接入，授权已记录；Screenshot 已采用风险分流，仅确认。project_strategy_migration_review 独立完成核对通过，两项目状态/阻塞/动作类型与迁移前一致。详见[摩托接入记录](/Users/jafish/Documents/work/motorcycle-manual-app/docs/data-quality/plan-risk-strategy-migration-20260908.md)和[Screenshot 接入确认](/Users/jafish/Documents/work/ScreenshotStitcher/docs/plans/first-release-roadmap.md#2026-09-08-风险策略接入确认)。未修改业务代码或执行实际业务门禁。

## 未决问题

### workset 最近证据限量输出

2026-09-06 用户反馈 `workset` 最近证据返回整张记录表，随后明确授权“你看着改吧”。本项作为阶段 1 内 E01/E02/E05 的有界后续改进实施，不新增阶段。风险判断为低风险：只读、显式选择、可撤销的呈现变化，默认契约与门禁保持，由当前实施者定向自验；已有独立失败均已解除，1.0.0 原独立技术结论仅覆盖原内容，不冒充本次增量验收。本次不发布、同步安装副本或提交 Git。

实测基线 `4734621`：`current_recent_evidence()` 返回当前阶段记录章节的全部数据行，`workset_payload()` 在完整 gate 派生后附加这些行；`run_workset()` 的 JSON 输出保留整表。普通文本和 hook 均不展示该表，因此此项首先优化 JSON 响应体积。当前计划返回 18 行，日期全部为 2026-09-06；按 `json.dumps(rows, ensure_ascii=False, separators=(',', ':')).encode('utf-8')` 计算为 6,402 字节，取 `rows[-3:]` 为 1,099 字节。该字段体积可减少约 82.8%，不是实测 token 或运行耗时收益；完整 workset stdout 为 11,385 字节，不能把字段缩减比例当作整体上下文缩减比例。

本次实施边界：

- 新增显式 `workset . --json --evidence-limit N`，N 为正整数；先按“每个返回计划的末尾 N 条”提供确定性窗口，不按同日日期排序，也不根据自由文本猜测哪些记录重要。窗口内保持原追加顺序，少于 N 条时全部保留。
- 默认不带参数时，现有 JSON schema、字段、行内容/顺序及退出码保持。只在序列化前裁剪显示，`current_recent_evidence()`、`workset_payload()`、gate、独立失败历史、blockers、next_action、warnings 和 hook 仍使用完整数据；裁剪不能清除有效阻塞或改变准入判断。
- 显式限量输出附带总条数、省略条数与实际原文位置，供调用者识别“不完整视图”。来源取地图登记的实际计划路径和实际匹配章节，不猜固定文件名；该显示元数据仅用于新模式，不混入 warnings，也不把记录表当成独立验收结论。
- 新模式在每个计划上增加 `recent_evidence_window`：`total` 为原记录条数，`omitted` 为省略条数，`source` 为包含 `path` 和 `section` 的对象。path 使用地图登记的实际计划路径（优先相对项目根目录），section 使用实际匹配标题；没有章节时为 null，重新读取失败或记录内容已变化、来源不能确认时 source 为 null。默认及底层 payload 不加此字段。非法 N 或参数组合由 argparse 报错并退出 2。
- 完整历史按需用原 `workset . --json` 或回到所指原文读取；`--include-history` 继续表示包含已完成/废弃等历史计划，与证据条数窗口分别处理，不能复用为“读取更多记录”。文本模式原本不展示证据，新参数用于非 workset/非 JSON 组合时应明确报错，避免无效参数静默成功。
- 文档与 skill 在实际实现时说明：末尾 N 条是恢复阅读入口，不保证包含所有验证所需证据；缺失关键上下文、存在阻塞或需要确认验收结论时，按来源继续读取。此改动缩减输出，不省略原始解析和门禁检查。

实现前/后的适用验证：默认 JSON 逐项兼容；18→3、空表、N 大于行数、同日/日期乱序；限量隐藏旧行时有效独立失败仍阻塞，默认/严格退出码与完整模式相同；非法 N/参数组合；与 `--include-history`、历史兼容标题和自定义计划路径组合；真实 Node→Python 调用、查询前后文件字节保持。验证应比较相同输入下的响应字节，分别报告字段及整体大小，不以规则文字推测 token 或时长。

实施前基线：已安装 1.0.0 与仓库实现对应，实际命令为 `node bin/plan-governance-cli.mjs workset . --json`，解析 `plans` 中本计划的 `recent_evidence` 后按上述 UTF-8 口径比较全表和末三行；新参数此前不受支持。该只读输出及待新增的行为回归作为 Step 0，不凭旧全量测试宣布新功能通过。

当前范围为 `scripts/check_plan_governance.py`、`tests/test_workset_evidence_limit.py`、`tests/npm_cli.test.mjs`、`resources/skill/references/cli.md`、README 及本计划/地图。完成条件为上述限量、兼容、阻塞与无写入样本通过，真实 CLI 输出对比可复验，文档说明“限量视图不等于完整证据”；有明确失败时修复并复验，影响扩大到门禁时重新评估风险。回滚仅撤销本次参数/显示层/文档差异，完整输出继续可用。

2026-09-06 实现与自验结果（基于 `4734621` 的本次工作树增量）：

- `PYTHONDONTWRITEBYTECODE=1 python3 -m pytest -q --no-cov -p no:cacheprovider tests/test_workset_evidence_limit.py`：28 项通过，覆盖追加顺序、空表、大 N、历史计划/标题、自定义路径、来源不可读、非法参数、有效独立失败仍阻塞及文件字节/mtime 保持。测试子代理 `/root/evidence_limit_tests` 参与测试实现，不记为独立验收者。
- `PYTHONDONTWRITEBYTECODE=1 node --test --test-name-pattern='workset evidence' tests/npm_cli.test.mjs`：2 项通过，真实 Node→Python 调用的完整/限量/回查、默认/严格退出码及参数报错通过。
- 同一当前文档输入下，将 `git show 4734621:scripts/check_plan_governance.py` 的旧代码与当前脚本作对照：默认/严格 × 活跃/含历史四组 stdout、stderr、退出码逐字节一致；限量输出还原证据字段并去除窗口元数据后，其余 payload 全字段一致。
- 当次对比（本表追加收尾记录前，18 条证据，下一动作暂为验证）：完整 stdout 11,372 字节，限量 3 条后含元数据为 5,277 字节，整体减少约 53.6%；本计划证据字段按上述紧凑 UTF-8 口径为 6,402→1,099 字节。后续新增记录会改变具体字节数；这不是 token 或执行耗时结论。可用 `node bin/plan-governance-cli.mjs workset . --json` 与追加 `--evidence-limit 3` 的命令复测当前输入。
- `PYTHONDONTWRITEBYTECODE=1 npm run verify`：退出 0，613 项 Python 测试通过，总覆盖率 93.57%，103 项 Node 测试通过、0 skipped；严格治理通过，原宿主回放、共享范围和背景引用告警保留其归属。
- `resources/skill` 的 `quick_validate.py` 检查通过，CLI 参考与 README 已说明窗口边界及 1.0.0 兼容方式；默认底层 payload、hook 与 gate 没有修改。

本项完成条件已由当前实施者确认，无新增阻塞。整体计划继续保留实际使用验收；本小改不增加用户逐次签收，旧独立报告不扩充为本次自验结论。仓库源码可用，已安装 1.0.0 仍不支持新参数；本次未发布、同步安装副本或提交。

后续发布授权及结果：用户要求“推送 1.0.1”，现已公开发布并确认 latest/包完整性，发布与 Git 推送由[1.0.1 发布维护](plan-governance-distribution-setup.md#2026-09-06-101-发布维护)记录；上述未发布说明保留为功能实施轮结束时的状态。

| 问题 | 推荐方案 | 是否阻塞当前阶段 | 状态 |
|---|---|---|---|
| B01 前置交接基线 | HEAD `7cd6953` 已包含前置独立技术交付，接手时工作树干净；本次用户明确授权推进。依赖条件据此改为已提交技术基线与推进授权，不新增追溯产品签收，不宣称历史接受 | 否 | 已解决 |
| B02 通用规则兼容与执行样本 | D1—D3 及执行矩阵经独立确认，阶段 0 设计完成、阶段 1 自身准入通过 | 否 | 已解决 |
| B04 模板式结论误放行 | 第三轮独立确认：开头未选择候选始终未知，问号/待选后缀等不再解除旧/新独立失败；历史失败保留 | 否 | 已解决 |
| B03 明确通过结论句号兼容缺口 | 第二轮独立实盘确认新/旧及保留旧记录均支持明确句号，已解除 | 否 | 已解决 |

## 风险和回滚

- 阶段 0 只改文档；阶段 1 修改已列明源码和资源。失败时只撤销本计划的具体差异，保持旧策略可用，不回滚共享工作区或改写前置结论。
- 前置技术工作已交付，提交或并行文档整理仍可能改变工作树；接手时刷新基线，不凭相同 HEAD 或旧报告宣布准入。
- 减少复核不能变成遗漏检查，拆参考文件不能丢失底线或分发资源；用正反场景验证，失败时保留阻塞并修复。
- 阶段 1 验证允许测试自行创建并清理临时 fixture、打包和临时安装产物；不触及真实服务、registry、全局 skill 或其他项目。规则读取失败不自动安装或修改项目。

## 关联资料

- 原交接报告和前置计划见背景；现有 grilling 与独立验收契约见计划边界表。
- 当前无独立 ADR/migration/spec 需要创建。确需跨迭代维护的新契约在阶段 0 判断，避免预建空文件或复制既有定义。
