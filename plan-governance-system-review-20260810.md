# 计划治理体系评审报告

> 评审日期：2026-08-10
>
> 评审对象：`motorcycle-manual-app` 的计划、路线图、阶段准入、证据、依赖和辅助治理机制
>
> 证据快照：目标项目 2026-08-10 的当前工作区，包含当日尚未提交的计划和进展文档。
> 评审性质：只读评审；本报告不代表代码实现授权，也未修改代码、测试或构建产物。

## 结论摘要

这套计划体系“能用，而且对高风险工作有效”，但已经从轻量计划治理演变成了“执行台账 + 验收档案库”。它在证据闭环、独立复核、事实源分层和回滚边界方面明显强于普通项目；在当前工作集可见性、历史内容隔离、计划粒度和多计划并行调度方面则偏重。

综合判断：

- 作为高风险数据、Schema、Agent、发布链路的准入与审计系统：良好。
- 作为日常开发者快速回答“现在做什么、谁被什么阻塞、哪些可以并行”的工作台：一般。
- 是否臃肿：存在明显的“主视图臃肿”和“单计划历史日志臃肿”，但不是所有证据都属于无效负担。
- 多计划并行支持：目前是低到中等。当前有 3 个活跃计划（1 个实施中、2 个设计中）；新增的搜索题库扩展计划证明体系能新开边界清晰的专项，但现有计划级依赖表和推荐顺序仍不足以表达阶段级并行。

最优先的改进不是继续增加门禁，而是把“当前工作集”和“历史证据”分开，并把依赖从“计划依赖”细化为“阶段依赖 + 依赖类型”。

## 评审范围与方法

检查了：

- [目标项目计划地图](/Users/jafish/Documents/work/motorcycle-manual-app/docs/PLAN_MAP.md)：索引、推荐顺序、依赖、替代/废弃、阻塞项和完成证据。
- `docs/plans/*.md`：全部 32 个专项计划，重点查看当前阶段、阶段准入摘要、Step 0、样本矩阵、验证、失败/回滚和独立复核记录。
- `docs/adr/*.md`、`docs/migrations/*.md`、`docs/specs/*.md`、`docs/fixtures/*.md`。
- `docs/data-quality/` 中的验收、基线、独立复核、回放和报告文件。
- `docs/graph/functional.yaml`、完成快照和项目级治理辅助配置。

执行的只读检查：

```bash
plan-governance-cli check .
plan-governance-cli check . --strict-readiness
plan-governance-cli check . --stale-days 10
plan-governance-cli check . --drift
plan-governance-cli check . --pre-commit
plan-governance-cli check . --check-attestations
plan-governance-cli graph validate .
```

## 现状快照

| 项目 | 现状 | 评语 |
|---|---:|---|
| 专项计划 | 32 个 | 数量本身可接受，但完成计划占绝大多数，主视图应收缩 |
| 计划状态 | 27 个已完成、2 个已废弃、1 个实施中、2 个设计中 | 当前真正需要调度的计划仍很少 |
| 计划正文 | 12,299 行；平均 384 行；中位数 319 行；P90 609 行 | 历史实施记录大量回写计划正文 |
| 最大计划 | `maintenance-module.md` 1,589 行；`ios-v1-mvp.md` 884 行 | 已接近领域档案，不再是轻量执行计划 |
| ADR / migration / spec | 4 / 2 / 4 个 | 数量少，且职责划分总体清楚 |
| 质量/验收材料 | 109 份 Markdown、178 个文件（另含截图和 CSV） | 证据丰富，但缺少“当前有效/历史替代”的统一索引 |
| `PLAN_MAP.md` | 199 行、约 58 KB | 索引中混入完成历史、长状态说明和过多证据细节 |
| 计划索引 | 32 行 | 覆盖完整，计划名未发现重复 |
| 显式依赖关系 | 依赖说明表 25 行，计划索引另有 32 个依赖字段 | 两处表达同一关系；有 5 个非根、非废弃计划只在索引行列依赖，未进入说明表 |
| 功能图谱 | 15 个节点、39 条关系，校验通过；GitNexus 引用为 0 | 能表达架构关系，尚未真正连接计划影响分析 |
| 完成快照 | 1 个 | `maintenance-data-adaptation` 的计划文件 hash 和 `PLAN_MAP.md` hash 均已漂移 |

所有治理检查均以退出码 0 通过，严格准入检查也通过；但 `--check-attestations` 报告 2 条完成快照 hash 漂移，`--drift` 对 5 个当天活跃计划治理文件发出覆盖 WARNING。这个结果说明机器可识别结构完整，不等于计划体系的导航、调度和事实新鲜度都没有问题。

## 分维度评价

| 维度 | 评价 | 等级 | 主要依据 |
|---|---|---|---|
| 事实源分层 | 计划、路线图、ADR、migration、spec、fixture、背景材料已经明确分工 | 良好 | `PLAN_MAP` 明确旧草案不作为事实源，多个计划也记录了反向引用检查 |
| 需求边界与非目标 | 大多数计划有目标、非目标、不变量和公共契约，复杂任务的范围控制较好 | 良好 | `data-release-validation`、`model-library-commerce`、`maintenance-module` 尤其完整 |
| Step 0 与样本证据 | 体系最强项；命令、输入、预期、失败判定和输出位置普遍存在 | 很好 | 32 个计划均能检索到治理字段；严格准入通过 |
| 独立复核 | 能保留失败、修复后复核和多轮复核，不轻易用实施声明替代验收 | 很好 | 首页重构、发布验收、车型商业化等均有追加式复核记录 |
| 状态与阶段一致性 | 规则设计正确，但“准入结论、实施进展、验收结论”没有并列呈现，读者容易混淆 | 中等 | 发布验收已补阶段 1 推进记录，但“最新独立准入复核”仍是实施前结论；完成快照已漂移 |
| 当前工作可见性 | 很难从一个入口快速得出当前待办、下一准入动作和可并行项 | 偏弱 | 推荐顺序主要是历史序列；“当前阻塞项”表包含大量已解除事项 |
| 依赖与并行调度 | 能表达粗粒度先后关系，不能表达阶段门、软依赖、可并行工作或依赖原因的变化 | 偏弱 | `data-release-validation` 和 `ios-app-release-readiness` 是明显的多输入汇聚点 |
| 计划粒度 | 高风险专项拆分合理，完成后的子计划和持续质量能力没有及时退出计划层 | 中等偏弱 | 32 个计划中 27 个已完成；多个计划内部又包含大量阶段实施日志 |
| 可复现性与可移植性 | 证据命令丰富，但存在开发机绝对路径、外部 sibling 仓库引用和通配符链接 | 中等偏弱 | 多个数据计划直接引用 `/Users/jafish/...`；地图含 `screenshots/...-*` 非具体文件链接 |
| 自动化与图谱联动 | 治理检查和图谱校验可运行，但尚未看到项目级 CI/Hook 接入，计划图也未接入功能图谱 | 中等 | `.github` 不存在；功能图谱校验显示 GitNexus 引用 0 |

## 做得好的地方

### 1. 准入体系不是形式化打勾

当前计划普遍保留目标、非目标、Step 0、样本矩阵、验证方式、失败/回滚边界和独立复核。尤其是 `data-release-validation`，已经把以下关键边界写成可验证契约：

- `candidate_id` 内容寻址和输入哈希绑定。
- `verified`、`not_applicable`、`needs_review`、`failed` 的发布影响。
- 报告 `result.passed` 的机器推导规则。
- 失败时不覆盖正式 ZIP、manifest、SQLite 和用户历史。
- 云端证据缺失、候选漂移和同车型升级不兼容时阻断发布。

这说明计划治理已经能承载真正的公共契约和安全边界，而不仅是任务清单。

### 2. 独立复核记录具有纠错能力

多处保留了“未通过 → 修复 → 再复核”的历史，例如首页 V2 导航经历多轮独立复核，发布验收阶段 0 也保留了三次复核。这比直接覆盖成“已通过”更可信，也符合追加式审计要求。

### 3. 事实源意识较强

`PLAN_MAP` 已明确历史草案、`docs/superpowers/` 和 Agent 方案只作背景；多个计划还主动执行了旧草案漂移和反向引用检查。Schema、商业化契约和 migration 也基本从计划正文中分离出来，减少了把代码方案写成永久契约的风险。

### 4. 阶段之间的安全边界清楚

很多计划明确写出“阶段 N 完成不自动进入阶段 N+1”。`model-library-commerce` 和 `data-release-validation` 对真实云资源、真实 Apple 验签、正式发布、安装、性能和撤回的边界尤其清楚，能够避免用 mock 证据冒充生产准入。

### 5. 新需求没有直接污染已完成计划

2026-08-10 新增的 `search-query-regression-library-expansion` 将 Q101–Q150 另立为阶段 0 计划，并明确 Q001–Q100 不变。这是正确的边界做法：原始回归库保留为已完成基线，新目标先完成来源审计和独立准入，再进入实现。它也说明当前问题不是“计划拆分太多”本身，而是主入口没有把活跃计划和历史计划分层展示。

## 主要问题与证据

### P0：主视图不是“当前工作台”，而是“路线图 + 历史台账”

`PLAN_MAP` 的计划索引当前只有 3 个活跃计划，但后面的“推荐顺序”“当前阻塞项”“完成证据”仍承载大量已完成历史：

- “当前阻塞项”中多行明确标记为“已解除”“已修复”“已完成”。
- “完成证据”从第 150 行起按阶段展开，重复了专项计划和独立复核报告的内容。
- 推荐顺序仍包含已完成和已废弃计划，没有独立的当前工作集、下一准入动作或可并行边界。多人协作时还可额外展示负责人和交接信息；单人项目不必为此新增必填字段。

结果是：新参与者打开计划地图后，能看到大量历史，但不能快速回答“今天应该推进哪一条、哪一条在等谁、哪些工作可以并行”。

### P0：准入状态、实施进展和验收状态没有并列入口

`data-release-validation` 的地图状态是“实施中”，正文当前阶段也写“阶段 1”，并已在 2026-08-10 追加阶段推进核查。其“最新独立准入复核”仍写“阶段 1 可开始实现”，从定义上它是实施前的准入结论，并不错误；问题在于读者若只看到“最新独立准入复核”，无法同时得知实施进度、下一次独立复核是否已经发生，以及阶段是否已达到完成条件。

因此应新增或统一引用“最近实施记录 / 最近验证记录”，而不是把实施前准入记录改写成实施完成结论。对活跃计划，地图的证据列应优先链接当前进展和当前阻塞，再链接历史准入结论。

同时，`--check-attestations` 报告：

```text
docs/attestations/maintenance-data-adaptation.json:
  maintenance-data-adaptation 计划文件 hash 已变化
  PLAN_MAP.md hash 已变化
```

该快照记录的阶段还是“阶段 5”，而当前地图/计划已经推进到“阶段 7”。快照机制已经发现漂移，但当前体系只输出 WARNING，不会自动告诉读者哪一个状态有效。

### P1：计划级依赖不足以支撑阶段级并行

当前依赖有两个问题：

1. 只有计划级依赖，没有“依赖哪个阶段”的字段。
2. 没有区分硬门禁、软依赖、信息依赖、证据依赖和可并行条件。

此外，计划索引中的依赖字段与“依赖关系”说明表不是同一份完整投影。以下计划在索引行有实际依赖，但没有对应的独立说明行：

- `cloud-conversation-multiturn`
- `cloud-conversation-maintenance-card-binding`
- `cloud-source-presentation`
- `maintenance-data-adaptation`
- `maintenance-fixed-node-coverage`

这未必是状态错误：`manual-data-pipeline` 是根计划，`maintenance-interval-semantics` 已废弃；但上述 5 个非根计划的遗漏会让人无法判断该表是否故意只覆盖部分关系。应明确表的范围，或让它成为唯一、完整的依赖说明来源。

当前三个活跃计划也暴露了串行化倾向：`ios-app-release-readiness` 的阶段 0 主要是产品身份、授权、隐私、签名和商店材料决策，其中相当部分可以与 `data-release-validation` 阶段 1 的验证器和逐车型回放并行；新建的 `search-query-regression-library-expansion` 阶段 0 也只做来源/目标审计，并不需要等待发布验收进入阶段 2。真正的硬门禁分别应后移到 Release Candidate 和 Q001–Q150 候选级发布验证接入时。

### P1：大计划内部混入了过多实施日志

`maintenance-module.md` 1,589 行，内部同时包含阶段 0、1A、1B、1C、2、2B、3、4 以及多轮实施前/实施后复核；`ios-v1-mvp.md` 884 行，`data-pipeline-parameterization.md` 660 行。它们已经更像领域演进档案。

这种写法的优点是上下文完整，缺点是：

- 当前阶段和历史阶段交错，检索成本高。
- 同一个字段、状态或契约容易在旧段落中出现多个版本。
- 计划文件变动频繁，完成快照和地图 hash 更容易失效。
- 新计划容易继续追加，而不是判断“应新开计划、更新 spec 还是记录一次验收”。

### P1：历史计划和活跃计划没有清晰的工作集隔离

`agent-qa-v3` 和 `maintenance-interval-semantics` 已明确标记“已废弃”，但仍与活跃和已完成计划一起出现在同一推荐顺序和主索引中。27 个已完成计划也继续占据主导航空间。

这不意味着历史计划应该删除。问题是“归档状态”只作为一列存在，没有对应的归档视图、历史索引或默认过滤规则。

### P1：证据多，但证据生命周期不够统一

质量报告和复核报告很丰富，但没有统一的“当前有效证据 / 被哪份证据替代 / 保留原因 / 过期条件”索引。发布验收已经在业务契约中设计了证据 hash 和 candidate 绑定，计划治理本身却只有一个完成快照示例，并且该示例已经漂移。新增的阶段 1 推进记录补强了可读性，但尚未形成通用的活跃计划证据索引。

建议把以下概念区分开：

- `latest_review`：当前阶段最新独立结论。
- `latest_evidence`：当前仍有效的运行证据集合。
- `historical_reviews`：仅供审计的历史记录。
- `attestation`：某个明确提交/候选的完成快照，不是永久状态源。

### P1：漂移检查没有覆盖活跃计划自身的治理变更

2026-08-10 对当前工作区运行 `plan-governance-cli check . --drift` 时，检查虽以退出码 0 通过，却对以下当天变更都发出了“未被活跃计划影响范围覆盖”的 WARNING：

- `docs/PLAN_MAP.md`
- `docs/plans/data-release-validation.md`
- `docs/data-quality/data-release-validation-stage1-progress-20260810.md`
- `docs/plans/search-query-regression-library-expansion.md`
- `docs/data-quality/search-query-regression-library-expansion-matrix-20260810.md`

这些都是当前治理活动直接产生的计划、地图或证据文件，并非典型的计划外代码改动。若每次正常推进都会触发同类 WARNING，实施者容易忽略真正的影响范围遗漏；反过来，若简单把整个 `docs/` 排除，又会掩盖计划与实际证据脱节。

建议为活跃计划建立最小化的“治理文件覆盖”约定：专项计划自身、其在地图中的索引项，以及明确声明的当前阶段证据输出应被识别为受该计划覆盖；`PLAN_MAP.md` 则可按计划 ID 或变更条目建立精确映射。先让这五类正常变更不再告警，再保留其他未声明文件的 WARNING，避免用宽泛忽略规则换取表面安静。

### P2：可移植性不足

多份计划的 Step 0 命令直接依赖开发者机器上的绝对路径，例如 `/Users/jafish/Documents/work/motofind/...`、`/Users/jafish/Documents/work/mineru-pdf-workflow/...` 和 Xcode 日志路径。它们在当前机器上有用，但不能直接在 CI、其他协作者机器或干净 checkout 中复现。

另外，`PLAN_MAP.md` 中的 UI 证据链接使用了 `screenshots/ui-design-system-stage2-*` 通配符。它不是一个可点击的具体 Markdown 目标，治理检查也不会发现这个问题。

### P2：功能图谱和计划图谱尚未连接

`plan-governance-cli graph validate .` 通过，说明功能图谱本身有效；但输出显示 GitNexus 引用为 0，YAML 也没有计划 ID、阶段 ID 或计划到功能节点的映射。因此它现在更像架构文档，而不是“计划变更影响哪些功能、哪些计划会冲突”的调度工具。

项目内也未发现 `.github`，只在代理规则中看到对检查命令的说明，没有看到项目级 CI 或 hook 自动调用这些治理检查。当前治理更多依赖实施者自觉执行。

## 是否臃肿：区分“有价值的证据”与“主视图负担”

结论不是“删掉大部分计划”，而是：

| 类型 | 判断 | 建议 |
|---|---|---|
| Step 0、契约、样本矩阵、失败边界 | 不臃肿 | 保留；这是本项目治理的核心价值 |
| 最新独立复核与失败复核记录 | 不臃肿 | 保留，但主索引只链接最新有效结论 |
| 已完成计划的阶段实施日志 | 偏臃肿 | 保留为历史，但从默认工作集隐藏或迁入归档视图 |
| `PLAN_MAP` 中的长完成证据和已解除阻塞项 | 明显臃肿 | 只保留摘要、最新证据链接和当前阻塞；历史详情下沉 |
| 已废弃计划的常规推荐顺序项 | 可剔除出主视图 | 保留废弃说明和替代关系，移到“历史/废弃”区 |
| 绝对路径、重复的状态摘要、通配符证据链接 | 可剔除/替换 | 改成可参数化命令、稳定相对路径和具体证据索引 |

## 建议剔除、合并或归档的内容

### 可以立即从“当前工作集”剔除

不是删除文件，而是从默认推荐顺序和当前工作区隐藏：

- `agent-qa-v3`：已废弃；多服务商和用量统计已明确另立未来计划。
- `maintenance-interval-semantics`：已废弃；当前采用固定节点累积口径。
- 27 个已完成计划：保留为事实/审计历史，但不再作为当前待办入口。
- 已完成阶段的重复“完成证据”段落：保留专项计划中的最新链接即可，历史表移到归档索引。

### 值得在下一次整理中合并为领域历史档案

合并目标是减少导航面，不是丢失证据：

- 数据基础历史：`manual-data-pipeline`、`data-directory-completion`、`data-directory-correction` 可整理成一条“数据基础建立与修正”历史链；`data-pipeline-parameterization`、`data-artifact-quality-gates`、`data-release-validation` 保持分开，因为后两者仍代表不同的发布门禁阶段。
- 搜索质量历史：`local-search-contract`、`search-coverage-quality`、`queryengine-match-quality` 可在历史导航上合并；`search-query-regression-library` 保留为已完成的 Q001–Q100 基线资产，新的 `search-query-regression-library-expansion` 继续承载 Q101–Q150 的增量工作。这正是“完成基线 + 新专项”优于重开旧计划的例子。
- Agent/云端对话历史：`agent-loop-hardening`、`agent-prompt-optimization` 以及已完成的云端对话子计划可保留各自最新契约，但在主路线图上归入“云端对话能力”一条领域轨迹。
- 保养历史：`maintenance-module` 作为领域主计划保留最新摘要；已完成的 `maintenance-next-service-display`、`maintenance-fixed-node-coverage` 和已废弃的 `maintenance-interval-semantics` 在主视图中只保留关系和结果。

### 不建议剔除

- ADR、migration、正式 spec 和 fixture：它们是公共决策、兼容边界和可执行验收契约，不应因为计划数量多而删除。
- `data-release-validation`、`ios-app-release-readiness` 和 `search-query-regression-library-expansion`：它们分别承载数据发布资格、App 上架资格和搜索回归扩展，职责不同，当前也确实有未完成工作。
- 最新独立复核报告、候选报告 JSON、发布 manifest/哈希证据：这些是验收可追溯性的基础。

## 多计划并行支持度

当前结论：低到中等；“少量并行”可以工作，“多条跨层工作流并行”还不够。

### 已具备的能力

- 计划索引有依赖字段。
- 专项计划有影响模块/文件、当前阻塞项和阶段指针。
- 严格准入要求阶段自带 Step 0、样本、验证和回滚边界，不会因为上游阶段完成就自动放行下游。
- 功能图谱和 GitNexus 可作为未来影响分析基础。

### 当前缺口

- 没有“下一准入动作”、优先级或并行边界；若是多人协作，再按需补负责人、交接信息和预计窗口。
- 没有区分“必须等待交付”和“只需要参考已有事实”。
- 没有阶段级依赖，因此容易把可并行的设计阶段也串行化。
- 没有显式标出计划之间的共享写入点，例如同一 Schema、同一 Tool 契约、同一发布脚本或同一 UI 路由。
- 依赖关系表与计划索引不完全同步，降低了调度可信度。

### 当前工作集的推荐并行化

在不改变业务范围的前提下，当前可以把工作分成三个泳道：

| 泳道 | 可推进内容 | 硬门禁 |
|---|---|---|
| 车型数据发布 | 完成 250 CL-C 同车型升级回放、逐车型云端只读证据、候选报告收口 | 阶段 2 才硬依赖客户端安装、性能、正式打包和撤回 |
| App 上架准备 | 冻结产品身份、授权审计、隐私数据流、签名责任、商店材料清单 | Release Candidate 阶段再硬依赖通过的数据发布候选 |
| 搜索回归扩展 | 审计 Q101–Q150 的来源、目标、车型适用性和样本矩阵 | 阶段 1 才硬依赖稳定的候选级回放输入；阶段 2 才接入发布验证 |

也就是说，`ios-app-release-readiness` 和 `search-query-regression-library-expansion` 的阶段 0 都不必整体等待 `data-release-validation` 完成；应把依赖后移到真正消费发布候选和发布验证结果的阶段。

## 推荐的治理结构调整

### 1. 给 `PLAN_MAP.md` 增加“当前工作集”，并缩短默认索引

建议主视图只保留：

```markdown
## 当前工作集

| 计划 | 阶段 | 状态 | 下一准入动作 | 当前阻塞 | 可并行/共享写点 | 最新进展 |
|---|---|---|---|---|---|---|
```

原有完整索引保留，但按“活跃 / 候选 / 已完成 / 已废弃”分组。当前阻塞表只放未解除问题；已解除项移到历史复核或专项计划。`负责人`、预计日期和排期只在多人协作或确有外部承诺时增加，不应成为轻量治理的默认必填项。

### 2. 把依赖改成阶段级、带类型的关系

依赖不应同时在索引行和说明表中手工维护。建议把“依赖关系”改为唯一、完整的边表；计划索引只显示由该表导出的简短摘要。边表至少增加：

```text
上游计划 | 上游阶段 | 下游计划 | 下游阶段 | 类型 | 解除条件 | 可并行阶段
```

`类型`建议使用：`hard_gate`、`contract`、`evidence`、`soft_context`、`shared_write_risk`。这样可以明确“阶段 0 可以并行，阶段 1 必须等待”的情况；不需要为所有历史计划补造阶段关系，只从活跃计划及其直接上游开始迁移。

### 3. 把完成计划从“计划执行层”降为“能力/审计层”

完成后只保留：目标、最终契约、最新完成条件、最新独立复核和回滚结论；详细实施日志和每轮失败证据通过历史记录链接保留。不要继续在已完成计划正文末尾追加新需求；新需求应新开计划或更新正式 spec。

### 4. 统一唯一状态源和快照语义

- `PLAN_MAP`：只负责计划状态、当前阶段、最后更新、依赖、当前阻塞、最近进展和最新证据链接。
- `docs/plans/*.md`：负责当前阶段事实源和实施细节。
- 独立复核报告：负责某次复核结论和证据。
- attestation：只证明某个时点的快照，漂移后必须明确标记“已过期”，不能继续被读作当前状态。

建议继续保留“最新独立准入复核”的结构化字段，但为活跃计划增加独立的“最近实施/验证记录”链接。前者回答“能否开始”，后者回答“目前做到哪里”；两者不应互相覆盖。`data-release-validation` 已有阶段 1 推进记录，可作为第一条迁移样例。

### 5. 给证据加有效期和替代关系

质量报告或复核报告至少登记：

```text
evidence_id | plan | phase | status | captured_at | valid_for | supersedes | expires_when
```

这样能减少“报告很多但不知道哪份有效”的阅读成本，也能让云端模型、Provider、数据版本或验证器变更自动触发证据失效。

### 6. 统一可移植命令

将 `/Users/jafish/Documents/work/motofind/...` 等输入抽象为：

- `UPSTREAM_ROOT` / `MINERU_ROOT` 环境变量；
- 仓库内 `docs/fixtures` 或 manifest 登记的输入快照；
- 命令中使用相对于仓库的路径；
- 对外部输入给出“缺失时的替代基线”，而不是让绝对路径成为唯一复现条件。

### 7. 建立项目级自动门禁

至少在 CI 或手动统一入口中执行：

```bash
plan-governance-cli check . --strict-readiness
plan-governance-cli check . --stale-days 10
plan-governance-cli check . --drift
plan-governance-cli check . --check-attestations
plan-governance-cli graph validate .
```

对 `--check-attestations` 的漂移维持 WARNING，并在该 attestation 被发布、合规或完成门禁引用时要求人工复核；不要因为普通历史快照漂移而阻断所有开发。`--drift` 应先补齐上述最小化治理文件覆盖后再接入统一入口，避免常规计划更新制造噪音。活跃计划的结构化准入缺陷继续交给 `--strict-readiness`，状态/依赖一致性检查可先以 WARNING 试运行，再决定是否提升。

### 8. 连接功能图谱与计划图谱

给计划增加可选的功能节点、代码路径或测试入口映射。这样修改公共契约前可以回答：

- 哪些活跃计划共享同一功能节点？
- 哪些计划同时修改同一 Schema/Tool/UI 路由？
- 哪些测试是多个计划的共同准入证据？

不需要让每个计划都绑定代码符号；只对公共契约、数据发布、跨层迁移和高风险共享模块绑定即可。

## 避免过度治理

- 不要为了归档而立即移动或重写 27 个已完成计划；先在 `PLAN_MAP` 中做默认过滤，等引用、检查器和链接迁移策略成熟后再处理物理归档。
- 不要要求每个计划都维护负责人、日期、风险分或功能图谱映射；这些字段只对多人协作、公共契约或共享写入风险较高的活跃计划启用。
- 不要把 attestation 漂移一律提升为失败；它首先是“快照已过时”的提示，只有被当作当前完成证据使用时才需要阻断。
- 不要把所有质量报告压缩或删除；应先建立最新有效证据入口，再决定哪些历史材料转入归档索引。

## 建议实施顺序

1. 先修当前状态可读性：补“当前工作集”，把已解除阻塞和完成证据下沉；为活跃计划同时链接最新准入结论与最近实施/验证记录，并人工复核漂移 attestation 的用途。
2. 再修依赖模型：声明依赖说明表的权威范围，补齐活跃计划及直接上游的边，并把 `ios-app-release-readiness`、`search-query-regression-library-expansion` 改为阶段级依赖。
3. 再做轻量归档：先把已完成/已废弃计划从默认工作集移出，保留路径、链接和最新结论；不立即删除或物理迁移审计材料。
4. 最后按收益启用自动化与图谱联动：先修正治理文件的漂移覆盖，再接入严格准入、停滞、图谱校验和 attestation 提示；之后只为高风险共享模块补计划—功能节点映射。

## 最终判断

这套体系不需要推倒重来。它已经建立了高质量的准入和审计能力，当前真正的问题是“治理信息没有分层展示”，导致轻量性和并行性下降。

如果只做三件事，建议是：

1. 主视图只展示当前工作集，历史计划和完成证据默认折叠/归档。
2. 依赖从计划级升级为阶段级，并标注硬/软依赖和可并行边界。
3. 为活跃计划并列展示准入、实施进展和 attestation 状态，并按证据用途做自动提示。

做完这三件事后，现有证据资产可以继续保留，同时显著降低计划阅读成本和多计划并行时的误排队风险。
